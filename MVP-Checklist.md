# MVP Checklist

Single source of truth for implementation status. Every agent and every human works from this
file. Rules for picking up a task are in `AGENTS.md`. Settled choices are in `DECISIONS.md`.

**Status legend:** `[ ]` not started · `[~]` in progress · `[x]` done · `[-]` cut or deferred

Every task lists **Tests first**: the failing tests to write before any production code.
Tasks within a phase run in order unless marked *(parallel)*. Phases run in order.

---

## Phase 0: Bootstrap and the one real unknown

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T0.1 | `git init`, MIT license, `.gitignore` (bin, obj, `.codemuster/ledger.db`, `node_modules`), `.editorconfig`, `.gitattributes` (LF everywhere), `Directory.Build.props` with `Nullable`, `TreatWarningsAsErrors`, `LangVersion latest`, `InvariantGlobalization`. | `[x]` | claude 2026-09-10 | Remote: https://github.com/TSCarterJr/CodeMuster (private until release). |
| T0.2 | Solution with the six source projects and six test projects from the AGENTS.md layout, all `net10.0`, xunit. Empty `Program.cs` that returns exit code 2 with usage text. | `[x]` | claude 2026-09-10 | xunit 2.9.3 + test SDK 17.14.1 via `tests/Directory.Build.props`; assembly name is `codemuster`; CLI e2e tests run `dotnet codemuster.dll` from the test output dir. |
| T0.3 | **Spike: SQLite + Roslyn in a self-contained build on all three OSes.** Console app opens a SQLite DB, writes one row, loads `fixtures/mixed-repo` solution with MSBuildWorkspace, resolves one invocation to its callee. Published `-r win-x64`, `osx-arm64`, `linux-x64` self-contained single-file. Record binary size and startup time in Notes. | `[x]` | claude 2026-09-10 | This de-risks D02/D03/D08. Result: single-file works, fallback not needed. Roslyn locates its build host relative to the exe, so `BuildHost-netcore/` and `BuildHost-net472/` must sit beside the binary (csproj target `KeepBuildHostBesideBundle` marks them `ExcludeFromSingleFile`); T12.1 must ship a zip/tarball per RID, not a bare exe. Sizes: win-x64 100.7 MB, osx-arm64 107.2 MB, linux-x64 100.1 MB. win-x64 startup-to-first-line 53-61 ms, whole run incl. solution load 3.5-3.7 s. Packages: Microsoft.Data.Sqlite 10.0.12, Roslyn 5.9.0, Build.Locator 1.11.2 plus `Microsoft.Build.Framework` 17.11.48 with runtime assets excluded (Locator MSBL001 otherwise). Only win-x64 was executed; the other two were published, not run. Spike lives in `spikes/`, outside the solution. |
| T0.4 | CI: `.github/workflows/test.yml` runs `dotnet test` on `ubuntu-latest`, `windows-latest`, `macos-latest`. | `[x]` | claude 2026-09-10 | Node must be on the runner for T8. No unit test possible for YAML; verified by the first push. setup-dotnet 10.0.x, setup-node 22, restore/build/test split so failures are attributable. |
| T0.5 | `fixtures/mixed-repo`: tiny C# minimal API (2 endpoints, one service behind an interface registered in DI, one repository, one shared helper, one hosted service, one dead method) plus a TS Next.js-shaped folder (2 `page.tsx`, one component, one hook, one barrel re-export, one API client). Planted defects: one missing tenant filter in a query, one unescaped HTML render. A `Migrations/` folder and a `*.g.cs` file that must be excluded. Must build with `dotnet build` and `npm ci` (typescript only). | `[x]` | claude 2026-09-10 | Golden expected call graph lives beside it as `expected-map.json`, written by hand in T7/T8. Deviation on Tim's instruction: the API is an MVC `[ApiController]` with `[HttpGet]` actions, not minimal API; T7.4 needs a second small fixture for `Map*` detection. typescript pinned 5.9.3 (npm latest is 7.x). Fixture has its own empty `Directory.Build.props` so it does not inherit ours. Planted: `QuoteRepository.ListForTenant` filters on Status only; `QuoteTable.tsx` renders customer via `dangerouslySetInnerHTML`. |

## Phase 1: Domain

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T1.1 | Models: `FileRecord` (path, language, content_hash, size, mtime_at_analysis, first_seen, last_seen, last_commit, last_commit_at, excluded_reason, deleted_at, summary, summary_hash), `Unit` (id, kind, key, fingerprint, status, fidelity, summary, summary_hash), `UnitMember` (unit_id, path, symbol, member_hash, distance), `Run`, `Analysis`, `Finding`. **Tests first:** record equality, `Unit.Fingerprint` is deterministic over member order. | `[x]` | claude 2026-09-10 | Kind is a closed enum: `File, Slice, Orphan, Verify`. Status adds `Retired` for units whose key left the tree (never handed out, never deleted). `Unit.LensHash` records the lens hash of the analysis that made it Done so scan can mark lens changes stale. `Finding` lives in T1.2. Language is a string constant, not an enum. |
| T1.2 | Findings schema (D11) as a Domain type plus JSON serialization contract. **Tests first:** round-trip; rejects missing required fields; rejects unknown severity; accepts the exact sample response the skill will ask the model to produce. | `[x]` | claude 2026-09-10 | The sample JSON in the test is the same text that goes into SKILL.md: `AnalysisResponseJson.Sample`, pinned byte-identical through a serialize round-trip. `DomainJson.Options` sets LF newlines and relaxed escaping so that holds on Windows too. Confidence is a 0..1 number. |
| T1.3 | Interfaces: `ILedger`, `ISourceTree` (list files with hashes and stat info, read file, HEAD commit, last commit per path), `ICodeMapper` (repo path -> `CodeMap`), `IAgentAdapter` (unit pack -> raw text), `IClock`, `IFileSystem`. **Tests first:** none (interfaces), but every later Application test uses fakes of these. | `[x]` | claude 2026-09-10 | Keep these small. Add a method only when a use case needs it. Adds `IContentHasher` (git blob hash of a dirty file) and `SourceFile` (KnownHash null when dirty). The ledger is storage only: stat cache, staleness, and retirement decisions live in `Scan` so they are tested with fakes; `RecordAnalysisAsync` is the one atomic write that also moves the unit. Fakes live in `tests/CodeMuster.Application.Tests/Fakes`. |
| T1.4 | `CodeMap` model: `Symbol` (id, path, range, kind, signature, body_hash), `Edge` (from, to, kind: Call/Implements/Overrides/Imports), `EntryPoint` (symbol_id, kind, display e.g. `GET /v1/quotes`), `ResolutionStats` (resolved, unresolved, top unresolved names). **Tests first:** JSON round-trip against `fixtures/mixed-repo/expected-map.json`. | `[x]` | claude 2026-09-10 | Golden file arrives in T7.1/T8.1; until then the round-trip runs on an inline map. `Symbol.Kind` and `EntryPoint.Kind` are free strings chosen by the mapper; `EdgeKind` is the closed enum. |
| T1.5 | Language and exclusion rules: extension -> language map; generated-file filter (`*/Migrations/*`, `*.g.cs`, `*.Designer.cs`, `*.min.js`, lockfiles, `*.d.ts`, snapshots, `linguist-generated`). **Tests first:** table-driven cases for every rule, including a Windows-style input path normalized first. | `[x]` | claude 2026-09-10 | Also `RepoPath.Normalize` and a gitignore-style `Glob.IsMatch` (used by lens globs in T3.6). `Migrations` and `__snapshots__` match directory segments only, case-sensitive; suffix and binary-extension rules are case-insensitive. Adds a `binary` reason for image/archive/font extensions so packs never contain bytes. Follow-up after the first real run: `.codemuster/**` is excluded as `tool-config` so the tool never audits its own config. |

## Phase 2: Infrastructure, ledger and git

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T2.1 | SQLite `ILedger`: schema creation and migration table, `files`, `units`, `unit_members`, `runs`, `analyses`, `findings`. Busy timeout set. **Tests first:** creates schema in a temp dir; opening twice is idempotent; `PRAGMA user_version` gates migrations. | `[x]` | claude 2026-09-10 | Raw SQL, no ORM. Microsoft.Data.Sqlite 10.0.12. WAL, busy_timeout 5000, `Pooling=false` so Dispose releases the file on Windows. Migration runs inside an IMMEDIATE transaction and re-reads user_version, so two processes opening a fresh db cannot both create tables. One `InternalsVisibleTo` seam so tests can read the connection's pragma. |
| T2.2 | `ILedger` file upsert and stat cache (D05). **Tests first:** unchanged mtime+size skips hashing (fake hasher call count is 0); changed mtime with same hash keeps analysis and updates mtime; changed hash invalidates every unit that has the file as a member; a record written in the same second as the file's mtime is flagged for rehash. | `[x]` | claude 2026-09-10 | Those four behaviours live in `Scan` and are tested there (T3.1, fakes); the ledger is storage. Ledger tests: upsert inserts and replaces every column, nullable columns round-trip, excluded and deleted rows are returned. |
| T2.3 | `ILedger` units: insert, mark stale by member hash, `next(batch)` returns oldest not-done or stale first, `done` records analysis and findings atomically. **Tests first:** `next` never returns a done unit whose fingerprint is current; `done` on a unit whose fingerprint changed since `next` is rejected with a clear error; two writers with busy timeout don't corrupt. | `[x]` | claude 2026-09-10 | The fingerprint check is in `Done` (T3.4); the ledger's `RecordAnalysisAsync` is the atomic write (analysis + findings + unit status). `NextAsync` orders by an insertion `seq` kept across re-upserts; member lookups chunk ids by 500. Indexes on `units(seq)` and `unit_members(unit_id)`. Runs are stored here too. `RecordAnalysisAsync` on an unknown unit id updates zero rows; callers check the unit first. |
| T2.4 | `ILedger` deleted-file handling: files absent from the tree get `deleted_at`, their units go stale, rows are never deleted. **Tests first:** as stated. | `[x]` | claude 2026-09-10 | Test-only commit: no DELETE exists against `files` or `units`, and `NextAsync` already skips Retired. Scan sets `DeletedAt` and retires units (T3.1). |
| T2.5 | Git `ISourceTree`: `git ls-files -s` parse (mode, blob SHA, path), `git status --porcelain` for dirty files, `git rev-parse HEAD`, single `git log --name-only --format=%H%x00%cI` walk to fill last commit per path. **Tests first:** against a temp repo built in the test with three commits; a dirty file gets our own hash, not the index SHA; paths come back with forward slashes on every OS. | `[x]` | claude 2026-09-10 | One git process per command, never per file. Four processes per scan: `ls-files -s -z`, `status --porcelain -z`, one `log --name-only` walk with `core.quotePath=false`, and `check-attr linguist-generated --stdin`. A dirty file reports `KnownHash` null and Scan hashes it (T1.3 design); a staged-only edit reports the new index SHA. Paths git still C-quotes (`"`, `\`, control chars) get `LastCommit` null. Conflicted entries collapse to one dirty row. Tests build a real 3-commit repo per test; ~8 s on Windows. |
| T2.6 | Content hasher for dirty files that matches git's blob SHA for the same bytes. **Tests first:** hash of a known string equals `git hash-object` output. | `[x]` | claude 2026-09-10 | So the ledger never holds two hash schemes. `GitBlobHasher.Hash(bytes)` plus `HashFileAsync`; CRLF bytes hash as-is (no normalization). Also adds the trivial `SystemClock` and `PhysicalFileSystem` adapters the composition root needs. |

## Phase 3: File units, the coverage floor

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T3.1 | `Scan` use case, file mode: discover, apply exclusions, upsert files via stat cache, create or refresh one `File` unit per included file, record `Run`. **Tests first (fakes):** first scan creates N units; second scan with nothing changed creates 0 and hashes 0; one changed file produces exactly one stale unit; excluded files appear in `files` with `excluded_reason` and no unit. | `[x]` | claude 2026-09-10 | Also carries the stat-cache and staleness tests moved out of T2.2-T2.4: same-second rehash (D05), changed hash -> Stale, deleted file -> `DeletedAt` plus Retired unit with its member rows kept, lens change -> Stale. A retired unit whose file returns starts over as Pending. `UnitsStale` counts every currently stale unit, not only newly stale ones. |
| T3.2 | `Status` use case: analyzed / total at HEAD, stale count, excluded count, resolution rate when present, low-fidelity count. **Tests first:** numbers match a seeded ledger; output is plain text and stable for the CLI test. | `[x]` | claude 2026-09-10 | Four fixed lines (`analyzed a/b at <sha7>`, `stale`, `excluded`, `low-fidelity`) plus `resolution` only when a run recorded a rate. Before any scan the first line ends with `no scan yet`. |
| T3.3 | `Next` use case: returns unit pack(s) for a `File` unit: path, language, content, lens instructions, findings schema sample, expected response format. **Tests first:** pack for a file unit contains the file content verbatim; `--batch 3` returns 3 distinct units; empty when complete. | `[x]` | claude 2026-09-10 | Pack is one markdown document written to stdout or `--out <path>`. Layout is pinned byte-for-byte by a test: header list (unit, kind, fingerprint, lenses), one `### <lens>` block per applicable lens, one fenced block per member (four-backtick fence when the content has three), the `AnalysisResponseJson.Sample`, and the exact `codemuster done <unit> --fingerprint <fp> --findings <file>` line the agent must run. Members render generically (symbol, distance) so slices reuse it. |
| T3.4 | `Done` use case: parse response JSON (T1.2), validate every finding path exists in the unit's members, store analysis, findings, summary, fingerprint. **Tests first:** valid response marks done; a finding for a path outside the unit is rejected; invalid JSON records a failed analysis and leaves the unit not-done. | `[x]` | claude 2026-09-10 | `done <unit> --fingerprint <fp> --findings <file>`: the fingerprint printed by `next` travels back on the command line, so no checkout state lives in the ledger; a mismatch is rejected with nothing recorded. Finding paths are normalized (`./`, backslashes) before the member check. Lens hash stored on the analysis is `Config.HashOf` of the lenses applying to the members. |
| T3.5 | `Estimate` use case: bytes / 4 per pending unit, total and per-kind. **Tests first:** matches seeded sizes. | `[x]` | claude 2026-09-10 | Pending = Pending, Stale, or Failed units; tokens = sum of member file sizes / 4 per unit plus a 700-token allowance per unit for the pack scaffolding and the reply (added 2026-09-11 after the first real run showed bytes / 4 alone was several times too low); one line per kind present plus `total ~N tokens`. |
| T3.6 | Lens config: `.codemuster/config.json` with named lenses (instructions, file globs, language filter), lens hash stored on each analysis; changing a lens marks its units stale. **Tests first:** hash changes when text changes; stale propagation. | `[x]` | claude 2026-09-10 | Ship one default lens: general correctness and security. Done before T3.1 because Scan, Next, and Done all consume it. `Lens.Applies` uses globs (T1.5 `Glob`) and language codes; `Config.LensesFor(files)` picks the lenses for a unit and `Config.HashOf` is the lens hash stored on the analysis. Stale propagation is a Scan test (T3.1). Missing config falls back to `Config.Default` until T3.8 turns that into the init gate. Fixture gets `.codemuster/config.json`. |
| T3.7 | CLI verbs `scan`, `status`, `next`, `done`, `estimate` with hand-rolled parsing (D18). **Tests first (end-to-end, real binary, fixture repo):** `scan` then `status` shows `0/N`; `next` writes a pack; `done --findings f.json` then `status` shows `1/N`; unknown verb exits 2 with usage. | `[x]` | claude 2026-09-10 | E2E tests copy `fixtures/mixed-repo` to a temp dir and `git init` it (porcelain paths are root-relative, and the ledger must not land in the source tree). Repo root is resolved with `git rev-parse --show-toplevel`, so verbs work from any subdirectory; outside a repo the git error prints and exit is 1. `done` exits 1 on Rejected or InvalidResponse. `next` with nothing pending prints a hint on stderr and exits 0. Fixture yields 20 units, 5 excluded. |
| T3.8 | `Init` use case and setup gate (D24). `init` writes `.codemuster/config.json` (default lens from T3.6) and handles `.gitignore` per D24. Every other use case except `doctor` fails with `NotInitialized` when config is missing; the CLI maps it to exit 2 and the `run codemuster init` hint. **Tests first (fakes):** init in an empty repo creates config and appends the ledger line; init is idempotent; existing `.gitignore` already covering the ledger is left byte-identical; `--no-gitignore` leaves `.gitignore` alone; `scan`/`status`/`next`/`done` on an uninitialised repo return `NotInitialized`. **E2E:** binary `scan` in a fresh fixture copy exits 2 with the hint; after `init --yes` it exits 0. | `[x]` | claude 2026-09-11 | The gate is `ConfigLoader.LoadAsync` throwing `NotInitializedException`; every verb but `init` loads config in the composition root, so no use case can be constructed uninitialised. `init` asks through a callback, so the TTY prompt stays in the CLI: `--no-gitignore` answers no, `--yes` or redirected stdin/stdout answers yes, otherwise `[Y/n]`. "Already covered" is decided by `git check-ignore -v`, counting only rules from ignore files inside the repo (not `.git/info/exclude` or global excludes, which teammates do not share) and not negated matches; an adversarial review caught the earlier text heuristic accepting a leading-space line that git does not honour. Appends `ledger.db` and `ledger.db-*` (WAL/SHM). `IFileSystem` gains `WriteAllTextAsync` (creates parents, UTF-8 without BOM). Fixture no longer commits `config.json`; e2e tests run `init --yes` first. The written config is untracked until the user commits it, so scan never audits it. |

## Phase 4: Skill and interactive mode

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T4.1 | `skill/SKILL.md` in Agent Skills format: run `scan`, loop `next` / analyze / `done` until `status` is complete, the exact response JSON sample from T1.2, and the rule "the pack is the scope; you may read more, but every finding must cite a path in the pack." **Tests first:** the JSON sample in SKILL.md is byte-identical to the T1.2 test fixture (test reads the file). | `[x]` | claude 2026-09-11 | Keep it under one screen. Logic lives in the CLI. 41 lines; test also pins the frontmatter and that every verb of the loop is named. Packs go to stdout and the response JSON to a temp file the agent picks, so nothing untracked lands in the repo. |
| T4.2 | `skill install --for claude\|codex\|gemini\|opencode [--global]` writes SKILL.md to the right folder. **Tests first:** each target writes to the expected relative path in a temp home / temp repo; running twice is idempotent. | `[x]` | claude 2026-09-11 | Paths verified 2026-09-11 (project / user): claude `.claude/skills/` / `~/.claude/skills/`; codex `.codex/skills/` / `~/.codex/skills/` (Codex source reads these first; its docs now lead with the cross-tool `.agents/skills/`, a two-line switch if wanted); gemini `.gemini/skills/` / `~/.gemini/skills/` (needs Gemini CLI >= 0.23; the local 0.22.2 predates skills); opencode `.opencode/skills/` / `~/.config/opencode/skills/` (plural). SKILL.md is an embedded resource so a global install carries it. Writes only when content differs. `--global` needs no git repo and takes the home folder from USERPROFILE on Windows and HOME elsewhere, matching where the harnesses look. |
| T4.3 | Manual check: run the interactive loop in Claude Code and Codex on `fixtures/mixed-repo`. Record in Notes what each harness needed. | `[x]` | claude 2026-09-11 | Not automatable. Tim or an agent in a real session. Claude Code (2.1.268, a subagent given only SKILL.md): 20/20 units, every `done` accepted first time, both planted defects found (tenant filter high, `dangerouslySetInnerHTML` high) plus real ones (hardcoded tenant 1, missing `/customers` route, no error handling). Codex (0.154 `codex exec`, workspace-write sandbox): 20/20, both planted defects found, no CLI failures; it spawned sub-reviewers because the skill said "fresh context", now reworded. Feedback applied: sample path made fictional, `category` defined, loop terminator named, exclusions explained, temp file outside the repo. Not exercised: Gemini (local CLI 0.22.2 predates skills) and OpenCode. Follow-ups noted: `estimate` undercounts pack boilerplate; numbered lines in packs would save a re-read per unit. |

## Phase 5: Headless driver

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T5.1 | `IAgentAdapter` implementations: `fake` (returns canned JSON from a file, used by all e2e tests), `claude`, `codex`, `gemini`, `opencode`. Each knows its headless invocation and how to get the model's final text. **Tests first:** fake adapter round-trip; each real adapter builds the expected argument list (no process launched); on Windows the resolved executable ends in `.cmd` when only the shim exists. | `[x]` | claude 2026-09-11 | Flags verified 2026-09-11, pack always on stdin: claude 2.1 `--print --output-format text --permission-mode dontAsk --strict-mcp-config --no-session-persistence --tools Read,Glob,Grep` (stdout is the text); codex 0.154 `exec --sandbox read-only --skip-git-repo-check --ephemeral --color never --output-last-message <tmp> -` (text read from the tmp file); gemini `--output-format json --approval-mode default` (`response` field; a write/shell attempt makes gemini exit non-zero); opencode 1.15 `run --agent plan --format json` (last text part of the JSONL). `fake` rewrites a template's finding paths to the pack's first file; `CODEMUSTER_FAKE_RESPONSE` names the template. Windows resolution follows PATHEXT order, never a bare file. |
| T5.2 | `Run` use case: pull `next --batch j`, spawn `j` adapters, feed results to `Done`, loop until empty. Ctrl-C cancels in-flight units cleanly and leaves them not-done. Retry a failed unit up to N times then mark failed. **Tests first (fakes):** j=3 runs three concurrently; cancellation mid-run leaves the ledger consistent and a second `run` resumes at the right count; a unit that fails N times is reported and skipped, not silently dropped. | `[x]` | claude 2026-09-11 | Adapter calls run concurrently; `Done` and bookkeeping are serialized behind a semaphore because the SQLite ledger holds one connection. Batches are lock-step (`WhenAll` per batch); work-stealing is a later optimisation. A failed unit is re-handed immediately (no back-off); after N attempts it is excluded for the rest of the run and reported, ledger untouched. Any adapter exception counts as an attempt with nothing recorded, unless the run itself is cancelling. `ResponseText.ExtractJson` returns the first balanced JSON object, so braces in prose after the reply cannot break parsing. Order: taken before T5.1 because `IAgentAdapter` already existed in Domain and the driver is tested with a fake adapter, so the two ran in parallel. |
| T5.3 | CLI `run --agent X -j N [--lens L] [--force]`. Prints progress `done/total` per unit. **Tests first (e2e):** `run --agent fake` on the fixture completes all units; `--force` re-analyzes done units. | `[x]` | claude 2026-09-11 | Also `--attempts N` (default 3). `--lens` was built and then removed: the ledger keeps one lens hash per unit, so a one-lens run analyzed every unit and the next scan under the full config marked all 20 fixture units stale. A one-lens pass means keeping one lens in `config.json`; per-lens runs need per-lens analyses in the ledger first. Headless packs ask for JSON only and omit the `done` command. Progress line per attempt, written synchronously so lines stay in order: `done/total unit (attempt n): message`. stdout and stderr are UTF-8 when redirected. Exit 1 when any unit gave up or the run was cancelled. Unknown agent name is a usage error (exit 2). |

## Phase 6: Report

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T6.1 | `Report` use case, markdown: coverage header (analyzed/total at commit, stale, low fidelity), findings grouped by severity then path, unit summaries as an inventory table. **Tests first:** golden markdown for a seeded ledger. | `[x]` | claude 2026-09-11 | Adds `ILedger.GetCurrentFindingsAsync`: findings of the latest successful analysis per non-retired unit, with the fingerprint they were made against; a finding whose fingerprint no longer matches its unit is marked stale in the report. Severities in fixed order, empty ones omitted; units table ordered by key. Model text is flattened to one line and pipes are escaped in table cells, so a multi-line claim cannot inject headings or table columns. Order: taken ahead of Phase 5 while T4.3 needed live harness sessions; it only reads the ledger, so it ran in parallel with T5.1 and T5.2. |
| T6.2 | CLI `report [--out file]`. **Tests first (e2e):** after `run --agent fake`, report contains the planted findings. | `[x]` | claude 2026-09-11 | SARIF is a later task (L2). `report` prints to stdout or `--out <file>`; the e2e plants a finding through the fake adapter's template and checks it in the markdown. Wired in the same commit as T5.3. |

**Milestone A: file-mode v0.** Everything above works end to end on `fixtures/mixed-repo` on three OSes with `--agent fake`, and once for real with `--agent claude`.

Reached 2026-09-11. CI runs the whole loop with `--agent fake` on Ubuntu, Windows, and macOS. Real run with Claude Code 2.1.268: `run --agent claude -j 4` analyzed 20/20 fixture units in 4 min 44 s and recorded 19 findings, including both planted defects (the unscoped `ListForTenant` query and the `dangerouslySetInnerHTML` render). One unit needed a second attempt because prose after its JSON contained braces, which led to the balanced-object extraction fix. An adversarial review of Phases 4 to 6 confirmed 7 defects, all fixed. Its verifiers for 18 test-strength findings did not finish; 7 of those gaps were closed alongside the fixes and the other 11 are parked as L10.

## Phase 7: C# mapper (Roslyn)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T7.1 | Hand-write `fixtures/mixed-repo/expected-map.json` for the C# half: every method symbol, every call edge, implements edges, the two HTTP entry points and the hosted service entry point. | `[x]` | claude 2026-09-11 | Goldens were derived by hand, body hashes included. mixed-repo gained `EmptyQuoteService`, an `IQuoteService` that DI does not register, so T7.3 proves the binding picks `QuoteService` only. `fixtures/minimal-api` has `MapGet` and `MapPost` with method-group handlers and is restored in CI. This is the golden file. Get it right by reading the fixture, not by running the mapper. Lives at `tests/CodeMuster.Mapping.CSharp.Tests/golden/mixed-repo.json`, one golden per mapper so the C# and TS halves never conflict; ids, signatures, and edges follow D26. |
| T7.2 | `RoslynMapper : ICodeMapper`: locate SDK (`MSBuildLocator`), open `.sln` (or all `.csproj` if none), walk every method body, resolve invocations, object creations, and property accesses to symbols, emit `Symbol` with signature and body hash. **Tests first:** symbol set equals golden. | `[x]` | claude 2026-09-11 | Body hash is over the normalized syntax text of the member, so whitespace-only edits don't invalidate. It joins the declaration's tokens with single spaces, so comment-only edits leave it unchanged too, as in the TypeScript mapper. Only the two Roslyn workspace packages are needed: Roslyn 5.9 runs MSBuild in its own build host, so `Microsoft.Build.Locator` and `Microsoft.Build.Framework` are gone. `.slnx` loads like `.sln`, and each document is walked once, so multi-target projects and overlapping solutions don't double-count. |
| T7.3 | Interface dispatch: for a callee that is an interface or abstract member, add edges to every implementation found by `SymbolFinder`. When DI registration is detectable (`AddScoped<I, T>()` etc.), prefer that binding and mark the edge `Bound`. **Tests first:** the fixture's `IQuoteService` resolves to `QuoteService` only, via DI binding. | `[x]` | claude 2026-09-11 | Biggest source of "the tool missed the real code." Bindings match through each registered type's interfaces, so closed and open generic registrations bind; factory registrations and `AddHttpClient` fall back to `implements`. A `base.M()` call to a virtual declared in source still gets `overrides` edges to sibling overrides; watch for it at dogfood. Only members declared in the solution fan out: a call to `object.ToString()` or `IDisposable.Dispose()` would otherwise pull every override or implementation in the codebase into the flow. |
| T7.4 | Entry-point detection: Minimal API `Map*` calls, controller actions with HTTP attributes, `IHostedService`/`BackgroundService`, plus configurable regex patterns from config. Display string like `GET /v1/quotes`. **Tests first:** the three fixture entry points, with display strings. | `[x]` | claude 2026-09-11 | Not detected yet: actions inherited from a base controller, Razor Pages handlers, Blazor components, SignalR hubs, and gRPC services. A `Map*` call gives no call edge to its handler, which is its own entry point. Scope set 2026-09-11: `Map{Get,Post,Put,Delete,Patch}` with a literal route and a method-group handler, tested on a second fixture `fixtures/minimal-api`; lambda handlers and route groups are not detected yet. Controller actions without an HTTP attribute count as `ANY /{Controller}/{Action}`. Config patterns wait until dogfood shows an entry kind these rules miss. |
| T7.5 | `ResolutionStats`: count resolved vs unresolved call sites, top 20 unresolved names. **Tests first:** fixture resolves 100%; deleting `obj/` (unrestored) drops well below threshold and the mapper reports why. | `[x]` | claude 2026-09-11 | This is the health gate (D09). The premise does not hold as written: design-time builds keep framework references, so a project without packages resolves the same restored or not, and the `run dotnet restore` diagnostic is the only signal. The test adds one package reference and one call into it on a temp copy to prove the drop. |
| T7.6 | Cross-check sampler: for a sample of symbols, plain text search for the name; edges the mapper lacks but text finds are recorded as `possibly_incomplete` on the unit. **Tests first:** a deliberately hidden reflection call in the fixture trips it. | `[ ]` | | Deferred 2026-09-11 until dogfood: a reflection call planted on the dead method would make it reachable and break the T9.2, T9.4, and T9.5 tests, a per-unit `possibly_incomplete` flag needs another schema change, and name search on real C# (Get, Save, Format) would flag nearly every unit. T7.5's unresolved-name list covers the common failure first. |

## Phase 8: TypeScript mapper (compiler API) *(parallel with Phase 7)*

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T8.1 | Hand-write the TS half of `expected-map.json`: functions, components, hooks, the barrel re-export resolved to its origin, the two page entry points, the API client leaf. | `[x]` | claude 2026-09-11 | Lives at `tests/CodeMuster.Mapping.TypeScript.Tests/golden/mixed-repo.json`; ids, signatures, and edges follow D26. `body_hash` hashes the declaration's tokens with comments and whitespace dropped and JSX text collapsed, because the TypeScript printer keeps JSX text whitespace verbatim; for the fixture each hash is SHA-256 of the declaration lines with all whitespace removed, so the golden could be written by hand. |
| T8.2 | Embedded `map.js`: load `typescript` from the target repo's `node_modules`, create a program from `tsconfig.json`, walk call expressions and JSX element usages, resolve via the checker, print the code map JSON. **Tests first:** run under `node` against the fixture, output equals golden. | `[x]` | claude 2026-09-11 | Script is a resource inside `Mapping.TypeScript`, extracted to a temp file at run time. The golden matched on the first run. A temp sample project covers what the fixture lacks: classes, constructors, `new`, member tags, identifier arguments, route groups, and excluded pages. Known gaps: a tsconfig holding only `references` maps nothing without a warning, consts wrapped in `memo` or `forwardRef` are not symbols, and dynamic `import()` counts as an unresolved call. |
| T8.3 | `TypeScriptMapper : ICodeMapper`: locate `node`, extract script, spawn, parse. Entry points: `app/**/page.tsx`, `pages/**`, plus config globs. **Tests first:** same golden through the C# side; missing `node_modules` yields a clear diagnostic and low-fidelity fallback instead of an empty map. | `[x]` | claude 2026-09-11 | A missing `typescript` fails the whole TypeScript map with `run npm ci --prefix <folder>`, so one uninstalled package in a monorepo drops all TypeScript to low-fidelity file units through T9.1. A local `dotnet test` needs `npm ci --prefix fixtures/mixed-repo/web` first, as CI does. |

## Phase 9: Slices

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T9.1 | `CompositeMapper`: runs every applicable mapper for the repo's languages, merges code maps, falls back to `ImportGraphMapper` (regex imports, name-match calls, fidelity `Low`) for languages with no mapper or when a mapper fails. **Tests first:** mixed fixture yields one merged map; forced C# failure yields Low fidelity for `.cs` only. | `[x]` | claude 2026-09-11 | A plain function returning the merged map plus the mapped and failed languages; cancellation propagates instead of counting as a mapper failure. Merged unresolved names alternate between mappers by rank, drop duplicates, and stop at 20. The resolution rate is null when every mapper threw and 1.0 when mappers ran but found no call sites. `ImportGraphMapper` waits on a decision, since dropping it would change D08 and D09. Until then a language whose mapper throws keeps whole-file `file` units marked low fidelity (D25). |
| T9.2 | `SliceBuilder`: from each entry point, BFS over Call/Bound/Implements edges to collect method-level members with distance. **Tests first:** the `GET /quotes` slice equals the hand-listed member set from the golden; the dead method is in no slice. | `[x]` | claude 2026-09-11 | The walk passes through symbols in files that are not included, such as migrations and generated code, without making them members, and an entry point in such a file starts no slice. Route corrected 2026-09-11: the fixture's controller serves `GET /quotes` and `GET /quotes/{id}`, with no `/v1` prefix. |
| T9.3 | Token budget (D07): full body for members up to distance `d`, then signature + summary, then signature only, until the pack fits the configured budget. Pack lists every outlined member. **Tests first:** with a tiny budget the entry point is still full-body and the leaf helper is signature-only; the outlined list is complete. | `[x]` | claude 2026-09-11 | Default budget goes in config; tune during dogfood. `slice_token_budget` defaults to 24000, counted at four characters per token over the lines shown. Members go in distance order, then path, then line. Distance-0 members always stay whole; once a farther member does not fit, it and every later member shrink to their signature, so depth degrades strictly. The signature-plus-summary tier is skipped because nothing produces per-symbol summaries yet. |
| T9.4 | Slice fingerprint = hash over member `(path, symbol, body_hash)`; a change to one member marks only slices containing it stale. **Tests first:** editing the shared helper marks both endpoint slices stale; editing the dead method marks none. | `[x]` | claude 2026-09-11 | No production code was needed; the tests were proved by mutation, since hashing symbol ids instead of bodies turns them red. Distance is not part of the fingerprint, so a change that only moves a member's distance leaves a Done slice Done. A member's hash covers its body hash and its signature, because the signature carries the class header: adding `[Authorize]` to a class changes no method body but must re-audit every unit holding one of its methods. |
| T9.5 | Orphan pass: every included file with no member in any slice becomes an `Orphan` unit (file mode pack, flagged as unreachable). **Tests first:** the dead method's file becomes an orphan; coverage total = slices + orphans and every included file is a member of at least one unit. | `[x]` | claude 2026-09-11 | `QuoteRepository`'s constructor becomes an orphan because only DI calls it. The fixture has no whole-file orphan, so that case is tested on a fake map. Rule replaced by D25: orphans are per file and hold only the unreached symbols, or the whole file when none is reached; files with no symbols or no working mapper stay `file` units. Test: `QuoteService.ArchiveQuote` lands in `orphan:src/MixedRepo.Api/Services/QuoteService.cs` while the file's other methods stay in slices. |
| T9.6 | `Scan` in slice mode is the default when any mapper is available; `--mode file` forces file units. Health gate: below resolution threshold, slices are created but `status` refuses `complete` and prints the top unresolved names. **Tests first:** as stated, with the unrestored fixture. | `[x]` | claude 2026-09-11 | `scan` runs the Roslyn and TypeScript mappers unless `--mode file` is given, prints `5 slices, 3 orphans, 10 files, resolution 100.0%` for the fixture, and writes mapper diagnostics to stderr as warnings. The unrestored-fixture test could not be written as stated (see T7.5), so the gate is tested with fakes and a missing restore surfaces as a scan warning. A published scan of the fixture takes about 10 s, mostly MSBuild's design-time build. Application half done with fakes: Scan takes the mappers and the repo root and, without mappers, runs in file mode exactly as before. Retirement now covers every unit kind, so Phase 10 must carry verify units across scans. Status half done: `complete` prints only when every unit is done and the last scan's resolution meets `resolution_threshold` (default 0.9); below it status prints `incomplete: resolution X% is below the Y% threshold` and the top unresolved names. It also withholds `complete` while any unit is low fidelity, because a failed C# mapper leaves the rate at 100% when TypeScript resolved everything. |
| T9.7 | `Next` pack for a `Slice` unit: entry point display, members in call order with degradation, lens, schema. `Done` accepts findings on any member path. **Tests first:** pack for `GET /quotes` contains the handler and the service method bodies in order. | `[x]` | claude 2026-09-11 | The header gains `- key:` (the entry point display for slices) and `- outlined: N of M members` when anything shrank. Symbol members show only their lines, numbered with the file's own line numbers so findings cite real lines, and a whole member's label names its containing type header (`inside [ApiController] public sealed class QuotesController`) so class-level attributes such as `[Authorize]` reach the model; a range past the end of a changed file shows what remains. Rendering branches on whether a member has a line range, never on unit kind (D06). `Done` already accepted any member path. Tested with fakes; the real `GET /quotes` pack is checked end to end in T9.6. |
| T9.8 | `Status` and `Estimate` show `slices done/total`, `orphans done/total`, blast radius before a run ("N units stale, touching M endpoints"). **Tests first:** numbers match. | `[x]` | claude 2026-09-11 | Status prints one line per kind present, such as `slice 1/2, 1 stale`, which is the blast radius before a run. Estimate counts symbol members at ten tokens per line, since the ledger keeps line ranges rather than sizes, and caps a slice at the larger of the budget and its entry point. Status, Estimate, and Report now take the config. |

**Milestone B: slice-mode v1.** Fixture audited end to end as slices: `GET /quotes`, `GET /quotes/{id}`, the `ReminderWorker` background slice, the `/quotes` and `/customers` page slices, orphan units for unreached code such as `QuoteService.ArchiveQuote`, and file units for files no mapper covers.

Reached 2026-09-11 with `--agent fake`, on all three OSes in CI. The real CLI scans the fixture into those five slices, three orphans (`QuoteService.ArchiveQuote`, `QuoteRepository`'s constructor, and the unregistered `EmptyQuoteService`), and ten file units; the `GET /quotes` pack holds the handler, service, and repository bodies in call order under the controller's class header; the fake agent audits all 18 units and status reads `complete`. Real run 2026-09-11 with Claude Code 2.1.268: `run --agent claude -j 4` on a restored copy of the fixture completed all 37 units (18 analysis, 19 verify) on the first attempt in 9 min 17 s, and status read `complete`. It recorded 19 findings. Both planted defects were found and confirmed: the unscoped `ListForTenant` query (reported by two slices, hence L12) and the `dangerouslySetInnerHTML` render. Verify refuted 5, each with a sound reason (a loop that cannot throw in this code, a `/customers` endpoint that does not exist, a race that needs a tenant id that never changes, a 500 nothing on the path can raise, a worry about a future `ArchiveQuote`), and confirmed the other 14, several with a corrected impact. The same day, the skill path end to end: the Windows x64 package installed with `npm install -g` from its tgz files, `init`, `skill install --for claude`, then `claude -p "Audit this repository with CodeMuster."` allowed only `codemuster` commands and file tools. Claude Code ran scan, next, and done through all 32 units (18 analysis, 14 verify) in 9 min 18 s with no failed analysis, wrote its responses under `%TEMP%`, changed nothing in the repository, and status read `complete`. It found and confirmed both planted defects; verify confirmed 10 findings, marked 2 unsure, and refuted 2. T7.6 is deferred, and the import-graph fallback and storing the call graph wait on Tim.

## Phase 10: Verify pass

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T10.1 | `Verify` units: one per unverified finding, pack = finding + its unit's pack + instruction to refute. Response: `confirmed \| refuted \| unsure` with reason. `done` updates `verify_status`. **Tests first (fakes):** confirmed and refuted both persist; a refuted finding is excluded from the default report. | `[x]` | claude 2026-09-11 | Same `next`/`done`/`run` machinery (D06). Scan retires every live unit it did not plan (T9.6), so verify units must be carried forward by scan. Settled in D27: `done` creates `verify:<finding id>` for each recorded finding, with the reporting unit's members and `path:lines` as its key, and retires the verify units of the findings a re-analysis replaces; `scan` keeps a verify unit while its finding is current and the unit's code is unchanged since that analysis, and creates one for any finding without one. So plain `run` verifies every finding after the analysis units in the same run, roughly one extra agent call per finding, and its progress total grows as verify units appear. Verdicts live on the finding row (ledger schema 3). The report leaves refuted findings out, counts them in the header, and marks the rest confirmed, unsure, or unverified with the reason. The fake agent refutes findings below 0.5 confidence and confirms the rest. `"verify": false` in config turns verification off (D28). A re-analysis retires the verify units it replaces before it records, so no second writer can hand out a verify unit whose finding is gone; `next` treats one as a broken ledger and stops, naming `codemuster scan`. |
| T10.2 | CLI `verify` (alias for `run --kind verify`) and `report --include-refuted`. **Tests first (e2e):** with a fake that refutes one planted finding, the report drops it. | `[x]` | claude 2026-09-11 | `run --kind file\|slice\|orphan\|verify` works and counts only that kind, and `--force` with a kind re-stales only that kind. `verify` accepts the same options as `run` except `--kind`. The e2e plants a confident and a doubtful finding in every file, analyzes with `--kind file`, then `verify` refutes every doubtful one; the report counts them as not shown and `--include-refuted` brings them back with the reason. `report` now rejects options other than `--out`. |
| T10.3 | The report's unit inventory leaves out verify units, since each verdict already shows under its finding. **Tests first:** a ledger with a verified finding lists the analysis unit and not its verify unit, while the header still counts both. | `[x]` | claude 2026-09-11 | Added 2026-09-11 after the first real run: 19 of the fixture report's 37 inventory rows were verify units repeating their verdicts. The coverage header and `status` still count verify units. |
| T10.4 | Verify pack tells the model to refute a defect in code nothing can reach (D34), while code reached through dependency injection, reflection, routing, or a library's public API counts as reachable. **Tests first:** the verify pack's instructions carry the rule. | `[x]` | claude 2026-09-11 | Added 2026-09-11 from Tim's answer after the two real runs disagreed on `ArchiveQuote`. The rule also went into SKILL.md's verify step. The reachability exceptions keep verify from refuting code the mappers leave unreached, such as a constructor only dependency injection calls. Checked with Claude on the audited fixture copy: `verify --agent claude -j 4 --force` re-ran its 9 verify units in 77 s. `ArchiveQuote does nothing` went from confirmed to refuted, with a reason that checked dependency injection registrations, routes, and reflection for a caller. Every finding on reachable code stayed confirmed, including the `ReminderWorker` one, which was confirmed after checking its `AddHostedService` registration. |

## Phase 11: Doctor

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T11.1 | `Doctor` use case: for each language present, a functional probe (D09): .NET SDK found and solution loads and one symbol resolves; `node` found and `typescript` resolves and program builds; git present. Reports `working / loaded-but-empty / failed`, time to ready, and the fix command for each failure. **Tests first (fakes):** each of the three states renders the expected line; never spawns an install. | `[x]` | claude 2026-09-11 | Each probe runs the real mapper over the repo's included files, so time to ready is what `scan` will pay. `working` means symbols came back with no diagnostics; `loaded-but-empty` means no error and no symbols, with what to check; `failed` means the mapper threw or reported diagnostics. A missing restore counts as failed because design-time builds still return symbols without one (T7.5), so its diagnostic is the only signal. If git cannot list files the report stops there. The use case has no process dependency, so it cannot install anything. |
| T11.3 | `doctor` and `scan` show progress while they work, on stderr with the elapsed time, so a long mapping never looks hung: files listed, each solution and project loaded, each C# project and tsconfig read, mapped file counts in 10% steps, then planning and saving. `run` says it started before the first unit finishes. Stdout stays as it was. **Tests first:** fakes see the messages in order with the language prefix; the Roslyn and TypeScript mappers report loading and file counts on the fixture; the CLI prints elapsed-prefixed progress on stderr for `doctor` and `scan`. | `[x]` | claude 2026-09-11 | Added 2026-09-11: Tim's first ToolbagCRM `doctor` printed nothing for 85 s. `ICodeMapper.MapAsync` takes an optional `IProgress<string>`; `CompositeMapper` and `Doctor` put the language in front. Roslyn reports each workspace file, each project as MSBuild finishes resolving it (`ProjectLoadOperation.Resolve`, so a multi-targeted project reports once per framework), the dependency injection binding pass, each project as its files are read (its first file forces that project's compilation), and mapped counts at every 10% crossing. `map.js` writes progress lines to stderr with `fs.writeSync`, since an asynchronous pipe write from inside the synchronous mapping pass would arrive in one burst at the end; the C# side reads stderr line by line and keeps non-progress lines for the error message. Counts step by 10% instead of by time, so the mappers need no clock. The CLI writer is synchronous rather than `System.Progress<T>`, which would post lines to the thread pool out of order. `run` prints its start line on stdout with its other lines. |
| T11.4 | `run` output names the unit by kind and short key instead of the full symbol id, and says it once: `12/30 slice CustomDomainMonitorWorker (attempt 1): recorded 8 finding(s)`. **Tests first:** the writer renders kind and key; `done` messages no longer repeat the unit; the end-to-end run output still shows each kind. | `[x]` | claude 2026-09-12 | Raised by Tim watching the ToolbagCRM slice run, where each line carried a 120-character Roslyn id twice. The `starting <unit>` lines from T11.3 landed in fd37533 with T13.5. |
| T11.2 | CLI `doctor`. **Tests first (e2e):** exit 0 on a healthy fixture, exit 1 with the missing-restore message after deleting `obj/`. | `[x]` | claude 2026-09-11 | `doctor` needs no `init` (D24) and writes nothing. Outside a git repository it prints `git: failed` with git's message and exits 1. On the restored fixture both mappers report `working` in about 10 s. |
| T11.5 | Readable overview and command-specific help via `--help`, `-h`, and `help`; help works before repository setup and update help never downloads anything. **Tests first:** real CLI help outside Git, fix concurrency/stash/defaults, unknown command usage errors; launcher help bypasses build discovery. | `[x]` | codex 2026-09-13 | Taken ahead of deferred work at Tim's request for CLI polish during his ongoing fix run. 17 new CLI cases and three launcher cases failed before implementation; all 786 .NET tests and 15 launcher tests pass. |
| T11.6 | Show recorded fix outcomes and reasons beside code findings in the Markdown report. **Tests first:** fixed and declined outcomes render without changing verification verdicts; CLI report after a committed fix displays its recorded outcome. | `[x]` | codex 2026-09-13 | Tim asked whether committed fixes remain unfixed in the ledger. Live commit/ledger comparison matches; the report currently drops code finding fix outcomes. Included in requested CLI polish. Both outcome cases and the committed-fix CLI regression failed before implementation; all 788 .NET tests pass. |

## Phase 12: Release

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T12.4 | `--version` prints the bare version and exits 0, ahead of the setup gate (D24). The CLI project carries a development default; the release workflow sets the tag's version. **Tests first (e2e):** the output is a SemVer version with no `+` build metadata. | `[x]` | claude 2026-09-11 | Added 2026-09-11; T12.1's smoke test needs it. The project default is `0.1.0-dev`; the `+commit` suffix the SDK adds to the informational version is dropped. It runs before argument parsing, so it works outside a repository and before `init`. |
| T12.5 | npm launcher (D33): `npm/` becomes the `codemuster` package. Its `bin` runs the newest CodeMuster build it can find (the installed optional dependency or `~/.codemuster/versions`) with the same arguments, exit code, and stdio; downloads the platform package on first run when none is installed; and updates in the background at most once a day unless `CI` or `CODEMUSTER_NO_UPDATE` is set. **Tests first (`node --test npm/test`, added to CI):** platform package names; version ordering; the newest build is chosen; no update check under `CI`, the opt-out, or within a day of the last one; a download whose integrity does not match is discarded; a newer version from a local fake registry is unpacked beside the old one. | `[x]` | claude 2026-09-11 | Added 2026-09-11 for D33. Plain Node 22 with no dependencies: `fetch`, `crypto`, and the system `tar` (on Windows `%SystemRoot%\System32\tar.exe`, because Git for Windows puts a GNU tar first on PATH that misreads `C:`). The launcher reads the abbreviated packument for `dist-tags.latest` and for the platform package's tarball and sha512 integrity, unpacks into a staging folder, and renames it into `~/.codemuster/versions/<version>`, keeping the newest two. It ignores Ctrl+C while the build runs, since the terminal already delivers it to the build, and forwards SIGTERM and SIGHUP. A background update failure is written to `~/.codemuster/last-update-error`. |
| T12.1 | Release workflow: on a `v*` tag, cross-publish self-contained folder builds for win-x64, win-arm64, osx-x64, osx-arm64, linux-x64, and linux-arm64; pack each as `@codemuster/<platform>-<arch>` and the launcher as `codemuster`, all at the tag's version; attach the tgz files to a GitHub release. `workflow_dispatch` with a version input builds and smoke-tests without creating a release. No global tool and no `npm publish` (D33). **Tests first:** a smoke job on Windows, Linux, and macOS installs the packed tgz files with npm and runs `codemuster --version`. | `[x]` | claude 2026-09-11 | Amended 2026-09-11 per D33. A manual run (34632083508, version 0.1.0-dev.2) passed: one Ubuntu runner cross-publishes all six builds, and the two macOS builds still get an ad-hoc signature there; it packs seven tgz files, and `npm install -g` plus `--version` pass on windows-latest, ubuntu-latest, and macos-latest (arm64), with the executable bit surviving `npm pack`. The win-arm64, linux-arm64, and osx-x64 builds are built but never run. The first manual run failed because npm reads `packages/<file>.tgz` as a GitHub shorthand; the paths now start with `./`. The release job only runs on a tag. The original row asked for single-file builds and a dotnet global tool; single-file cannot hold because Roslyn's build host ships as folders. |
| T12.2 | README, skill first (D32): what it is, install with npm, `doctor`, the skill install line, then `scan`, `run`, and `report` for people driving it by hand, and how updates work and how to turn them off. | `[x]` | claude 2026-09-11 | Amended 2026-09-11 per D32 and D33. `README.md` at the repository root and `npm/README.md` for the package page, both leading with the skill. The commands, flags, and config keys they name match `Program.Usage` and `ConfigJson`. |
| T12.3 | Rename from working name once decided (D14): namespaces, folder, `.codemuster/`, tool id. | `[x]` | claude 2026-09-10 | Name chosen before any code existed; docs and folder renamed to CodeMuster (D23). |
| T12.6 | Document the supported audit-to-fix workflow, configuration, command reference, retries and recovery; keep the npm README aligned and add contributor setup. | `[x]` | codex 2026-09-13 | Prioritized at Tim's request for documentation while ToolbagCRM fixes run. Documentation only; verify commands against parser/help and configuration, and check local links. README copies match; all Markdown fences and repository links resolve. Help/parser/configuration cross-check completed. No new product decisions. |

## Phase 13: Dogfood

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T13.0 | `exclude` globs in `.codemuster/config.json` (D04): a file matching one is recorded with reason `exclude:<glob>` after the built-in rules, gets no unit, and stays out of `doctor`'s probes, so a first pass can cover one subtree. **Tests first:** older configs parse with an empty list; built-in reasons win; a newly excluded file's unit is retired and comes back when the glob is removed; doctor does not probe a language whose files are all excluded. | `[x]` | claude 2026-09-11 | Added 2026-09-11; T13.3 needs it. `exclude` is a property with an empty default rather than a constructor parameter, because the Domain JSON options require every constructor parameter without a default, and an older `config.json` must still parse. Globs match like lens globs: without a slash they match file names anywhere, so a folder is `folder/**`. There is no include list; a subtree pass excludes the rest. Doctor reads the config when the repository is set up and applies only the built-in rules before `init`. |
| T13.1 | `doctor` on ToolbagCRM; fix whatever it reports until both mappers are `working`. | `[x]` | tim 2026-09-11 | C# worked on the first run: 15,779 symbols in 85.2 s with no output until the end, which led to T11.3. TypeScript failed until `npm ci --prefix edge/tenant-domains-worker`; the other five tsconfig folders already resolved `typescript`. |
| T13.2 | Scan ToolbagCRM; record resolution rate, slice count, orphan count, top unresolved names in Notes. | `[x]` | tim 2026-09-11 | Before any exclusions (commit ab96c1e): 4,899 files (310 excluded), 4,617 units: 199 slices, 2,383 orphans, 2,035 file units; resolution 99.9%; `estimate` about 16.6 million tokens (orphans 7.1 M, files 6.3 M, slices 3.2 M). Slices are about 33 C# (the background workers and a few `Map*` calls with method-group handlers) plus the Next.js pages. Most API endpoints are minimal API calls with inline lambdas (112 of 137 `Map*` calls in Api and Infrastructure), which T7.4's entry points do not recognize, so endpoint code became orphans: 210 in `Api/Endpoints`, about 425 under the plugin folders, 630 in the test projects. The top unresolved names are TypeScript IIFEs recorded as their whole source text (L13). Tim's config now excludes `toolbagcrm-api/tests`, `toolbagcrm-api/demo`, `toolbagcrm-ui`, `toolbagcrm-mobile`, and `edge`. |
| T13.3 | One lens (tenant scoping) on one subtree with `run --agent claude`; record tokens, wall time, findings, verify results. | `[ ]` | | Decides budget defaults and whether verify is good enough. Nothing scopes a run to one subtree yet: `run` has no `--lens` (removed, see T5.3), and a lens's globs do not remove units, so every file outside them is still sent to the agent with no instructions. D04 lists exclusions in `config.json` but no task builds them; add one before this pass. |
| T13.5 | `--model` and `--effort` on `run` and `verify` (D35), passed to each harness's own flag, and every analysis records the agent, model, and effort (ledger schema 4). Report shows what audited each unit. **Tests first:** each adapter's argument list carries the model and effort it was given; gemini reports that it has no effort flag; a recorded analysis keeps its provenance through the SQLite ledger and a v3 database migrates; report names the agent and model per unit. | `[x]` | claude 2026-09-12 | Added 2026-09-12 from Tim: knowing that `luna` at low effort produced a finding is what justifies re-running it on a stronger model. Effort flags differ per harness: `claude --effort`, `codex -c model_reasoning_effort=`, `opencode --variant`, and gemini has none. Provenance rides on `Analysis.By`, set from `IAgentAdapter.Identity`, so the interactive `done` path records none. Schema 4 adds `agent`, `model`, and `effort` to `analyses`; the columns come only from the migration, since a fresh ledger applies the base schema and then every migration in turn. The report gains one header line, `audited by claude opus/xhigh (28 units), codex luna/low (2 units)`, which stays out of the report entirely when nothing recorded provenance, so no existing golden changed. What ran is not always what was asked for: a harness may fall back to another model, and reading back what actually answered needs `claude -p --output-format json`, left for later. |
| T13.6 | Agent pool (D36): `--agent` repeats with slots, model, and effort; one shared queue; per-agent health with cooldown; a failed agent's unit goes to another agent; the run stops cleanly when every agent is cooling down. **Tests first:** two fake agents with one slot each both take work; a failing agent's unit completes on the other; an agent in cooldown is handed nothing; all agents failing ends the run with its reason, not with every unit given up. | `[ ]` | | Depends on T13.5 for provenance. |
| T13.7 | SKILL.md teaches how to choose: drive `next`/`done` by hand only for a small scope; for a big repo use `run` with a pool and prefer agents other than the one the session itself runs in; ask which harnesses and models are available and get an okay on `estimate` first; never drive the loop by hand while a run is going; read results with `report --out`. **Tests first:** the skill test pins each rule. | `[ ]` | | Tim's point: a skill running inside Claude can send the bulk work to codex or gemini, which keeps the session's own limit and context free. |
| T13.8 | Smoke `codex`, `gemini`, and `opencode` on one real unit each and record what each needed. | `[ ]` | | None has ever launched from CodeMuster; the pool should not be built on untested adapters. |
| T13.9 | `--path <folder>` on `run`, `verify`, `fix`, and `estimate`: only units with a member under that folder, composed with `--kind`, and `--force` re-stales only what matches. Folder prefixes, not globs, since globs belong to `exclude`. **Tests first:** the ledger hands out only matching units; a slice that touches the folder counts; `--force` leaves other folders alone; `estimate` prices the scope. | `[x]` | claude 2026-09-12 | Added 2026-09-12 from Tim: with 1,963 C# units pending, starting the UI pass must not drag the C# backlog along. The filter lives in `ILedger.NextAsync`, so the queue never hands out work outside the scope; `run --force` and the per-batch total reuse one `UnderPathAsync` helper over unit members. A folder matches a member exactly or as a prefix with a slash, so `--path web` leaves `webbing.md` alone. A slice that touches the folder counts, even if its other members sit elsewhere. |
| T13.13 | `codemuster update` brings the install up to the newest published version, and `update --check` only says what is available. **Tests first:** a newer version is fetched and unpacked so the next command runs it; the newest version downloads nothing and asks the registry once; `--check` reports without installing. | `[x]` | claude 2026-09-13 | Added 2026-09-13 from Tim: `npm i -g codemuster` is the only way to move forward today, which needs a writable global prefix and does not say what changed. The npm launcher already owns downloads, sha512 integrity checks and `~/.codemuster/versions`, so it answers the verb itself before the build is spawned, and writes `last-update-check` so the daily background check does not repeat the work. |
| T13.10 | A unit that returns from retirement keeps its Done status when its fingerprint and lens hash still match, instead of being analyzed again. **Tests first:** exclude a file, scan, re-include it, scan, and its unit is Done with its analysis intact; a real code change still marks it stale. | `[ ]` | | Today toggling `exclude` costs a second audit of everything re-included, which makes exclusions unusable for temporary scoping. |
| T13.11 | Whole-file units split into line-range chunks above a configurable size, keyed `file:<path>#<n>`, so one huge file is several units instead of one oversized pack. **Tests first:** a file over the limit plans N chunks covering every line with no overlap; each pack numbers its own lines; findings cite real file lines; a file under the limit stays one unit. | `[ ]` | | Raised 2026-09-12: ToolbagCRM's `src/lib/i18n/en.ts` is 365 KB, which is one ~90k-token pack today. Not UI-specific; any large file hits it. |
| T13.12 | A run stops when the agent itself keeps failing: after a configurable number of consecutive adapter failures with no unit recorded in between, stop and print the last error, instead of spending every unit's attempts on the same broken harness. **Tests first:** an adapter that always throws stops the run after the limit with its message and leaves the remaining units untouched; a failure followed by a success resets the counter; invalid responses, which are the unit's own fault, never trip it. | `[ ]` | | Raised 2026-09-12: a wildcard permission rule in ToolbagCRM's `.claude/settings.local.json` made every `claude` call exit 1, and the run burned three attempts on each of ten units before Tim noticed. The same shape covers an expired login or a provider outage, and it is the single-agent half of D36's cooldown. |
| T13.4 | Read every finding by hand and record precision in Notes. Feed lens and budget changes back as tasks. | `[ ]` | | |

## Phase 14: Fix mode (D37)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T14.1 | Write-enabled adapters: `AgentAdapters.Create(..., write: true)` adds `--permission-mode acceptEdits` and edit tools for claude, `-s workspace-write` for codex, `--approval-mode auto_edit` for gemini, and the `build` agent for opencode. No adapter gets shell access. **Tests first:** each adapter's argument list in write mode and in read-only mode; the default stays read-only. | `[x]` | claude 2026-09-12 | Flags confirmed from each CLI's own help on 2026-09-12. One `write` flag per adapter swaps the value in place, so the read-only argument order is unchanged and the existing harness tests still pin it. No adapter gains a shell tool in either mode. |
| T14.2 | `fix` units: one per file that has at least one confirmed finding, holding that file's findings and the members of the units that reported them. Planned only by the `fix` verb, never by `scan` or `run`, so no other command can write to the repository. **Tests first (fakes):** a file with two confirmed findings plans one unit; refuted, unsure, and unverified findings plan none; a fixed unit is not planned again while its code is unchanged. | `[x]` | claude 2026-09-12 | Adds `UnitKind.Fix`. A fix unit's one member is the file itself with its current content hash, so fixing the file marks the unit stale rather than planning it again, and the fixer edits a file it is shown whole. `run --kind fix` is rejected by the CLI, so only the `fix` verb can hand out these units. `run` excludes it by kind, which keeps the loop kind-agnostic apart from the pack (D06, D27). |
| T14.3 | Fix pack and response: the findings as JSON, the code, and the instruction to change what is wrong and say what it changed. Response: what was changed, which findings were addressed, and which were declined with a reason. `done` records `fix_status` and `fix_reason` per finding (ledger schema 5) and marks the unit done. **Tests first:** response round-trip; addressed and declined both persist; a response that addresses nothing is a failed attempt. | `[x]` | claude 2026-09-12 | Findings schema itself is unchanged (D11): the verdict columns sit on the row, as verification's do. The pack shows each finding with the id to answer with and the verifier's reason for keeping it, then the whole file. A response citing a finding from another file, or answering none at all, is rejected rather than recorded. Schema 5 adds `fix_status` and `fix_reason`. |
| T14.4 | `codemuster fix --agent <name> [--model] [--effort] [-j N]`: refuses a dirty working tree, plans and runs fix units, commits one per unit naming the findings, and never pushes. **Tests first (e2e, fake agent):** a dirty tree stops it with what to do; each unit becomes one commit; a declined finding is recorded and still counted; `status` and `report` show fix coverage. | `[x]` | claude 2026-09-12 | `IWorkspace` in Domain, `GitWorkspace` in Infrastructure: status, add and commit, and a restore that throws away a failed attempt's edits before the retry. Fix is serial by design, so it has its own loop rather than `Run`'s, and takes no `-j`. A unit that only declines is recorded without a commit, since nothing changed. The fake agent answers a fix pack by addressing every finding id in it, which keeps the end-to-end test free. |
| T14.5 | `"test_command"` in config runs after each fix unit; a non-zero exit is a failed attempt with its output as the reason. **Tests first:** a passing command records the unit; a failing one retries and then gives up with the output. | `[x]` | claude 2026-09-13 | Without a `test_command`, fix mode still runs and says it verified nothing. The command is a program and its arguments, `["dotnet", "test"]`, never a shell string (AGENTS.md). Tests run before `done` records anything, so a fix that fails them leaves no trace beyond a restored working tree: nothing recorded, nothing committed, and the attempt retried. A decline-only unit is tested too, since the agent may still have touched files. |

| T14.6 | Keep unrelated untracked files out of fix commits. **Tests first:** real Git commit includes only the tracked fix and preserves untracked files; CLI fix with a fake agent exercises the commit path without adding unrelated files. | `[x]` | codex 2026-09-13 | Taken ahead of remaining tasks at Tim's request after ToolbagCRM exposed the mismatch between the tracked-only clean check and `git add -A`. `git add --update` now matches the tracked-only workspace contract and preserves D37's dirty-tree refusal. All three regression cases failed before the fix; all 722 .NET tests pass after restoring the fixture dependencies as CI does. CLI regression uses the fake agent and a `git apply` test command to exercise an actual commit, asserting the exact committed path and preservation of the untracked inventory. |

| T14.7 | Offer stash-and-continue for dirty fix runs (D39), restore tracked edits and staging afterward, and provide `--stash` for noninteractive consent. **Tests first:** CLI refusal without consent and successful stash/fix/restore; prompt acceptance and decline; restoration on failure/cancellation; real Git staged/unstaged preservation, existing stash isolation, and recoverable restore conflicts. | `[x]` | codex 2026-09-13 | Taken ahead of remaining tasks at Tim's request to unblock fixing findings without manually preparing a clean checkout. 742 .NET tests pass. Real terminal smoke verified both decline without mutation and consent followed by a fix commit with staged/unstaged edits and untracked files preserved. Recovery stashes remain available; dirty submodules that git cannot stash stop the run. Local installation version: 0.2.2. |

| T14.8 | Parallel per-file fixing via `fix -j N` (D40). **Tests first:** CLI option validation; N concurrent calls with one file per worker and immediate slot refill; isolated worker diffs reject unrelated paths; coordinator serializes tests, scoped commits, and ledger writes; failure/retry isolation and cancellation before stash restoration. | `[x]` | codex 2026-09-13 | Taken ahead of remaining tasks at Tim's request after a real serial run took several minutes per file. 761 .NET tests pass. A real CLI smoke using a fake Codex executable proved `-j 10` starts exactly four overlapping workers for four files, integrates eight findings as four scoped commits, restores staged/unstaged local edits, and removes finished worker checkouts. No LLM calls. Local installation version: 0.2.3. |

| T14.9 | Ignore only the untracked Impeccable hook cache in isolated fix workers; report and retain rejected extra edits, and show retry attempts. **Tests first:** cache does not block the assigned patch; tracked cache and other new files remain rejected with exact paths and recoverable contents; retries end in a clear give-up message. | `[x]` | codex 2026-09-13 | Prioritized at Tim's request while documentation work is underway: live ToolbagCRM workers contain the intended source edit plus `.impeccable/hook.cache.json`. Preserves D40's one-file integration boundary. All six regressions failed before implementation; all 767 .NET tests pass. Installed 0.2.3 and the active ToolbagCRM run remain untouched. |
| T14.10 | Persist failed fix attempts, expose failure history in reports and feedback in retry packs, and teach the skill to recover failures and declines. **Tests first:** serial/parallel failure persistence, retry feedback, historical report reasons, SQLite reopen. | `[x]` | codex 2026-09-13 | Prioritized at Tim's request so the AI can inspect reasons and fix their causes. Reuses analyses; no findings schema change. Seven regression cases failed before implementation. All 799 .NET tests and 15 launcher tests pass; SQLite reopen and separate CLI failure/recovery processes preserve history. Skill validator passes; the shared skill stays within its 60-line limit. |

## Phase 15: Dependency vulnerabilities (D38)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T15.1 | Parsers turning each tool's JSON into one shape: package, vulnerable range, severity, advisory id and URL, title, fixed version, and whether it is direct or transitive. Covers `npm audit --json`, `pnpm audit --json`, yarn berry `yarn npm audit --json`, yarn classic `yarn audit --json` (one JSON object per line), and `dotnet list package --vulnerable --format json`. **Tests first:** real captured output from each tool parses to the expected set; an empty report yields nothing; unknown severity and a missing fix version are tolerated. | `[x]` | claude 2026-09-13 | Fixtures are real output captured on 2026-09-13: npm from ToolbagCRM's UI, pnpm and yarn classic from a throwaway project pinned to a vulnerable `minimist`, dotnet from a throwaway project pinned to vulnerable `Newtonsoft.Json` and `System.Text.Json`. pnpm and yarn still speak the npm 6 advisory format, so one parser serves both, and yarn classic wraps each advisory in a line of its own. Yarn berry's `yarn npm audit --json` is parsed by the same map reader but has not been run against a real berry project. NuGet's audit gives severity and an advisory URL but never a fixed version, so that field is null for C#. One package can carry several advisories: ToolbagCRM's `next` has two criticals. |
| T15.2 | `DependencyAuditor` in Infrastructure: finds manifests (package-lock.json, yarn.lock, pnpm-lock.yaml, and the solution or projects), runs each tool with an argument list, tolerates the non-zero exit these tools use when they find something, times out, and reports progress per manifest. **Tests first:** a fake runner drives each ecosystem; a failing or missing tool becomes a diagnostic, not an exception; a lockfile whose tool is absent is skipped by name. | `[x]` | claude 2026-09-13 | Never runs an install or a restore; reads only. A lockfile only counts when its `package.json` is also an included file, so an excluded folder is never audited. Solutions win over projects, so a repo with a `.slnx` is audited once rather than per project. Every failure path, including a missing tool, unreadable output, and an empty result, becomes a diagnostic naming the manifest. Five-minute timeout per tool. |
| T15.3 | `scan` records them: one `dependency` unit per manifest, always Done, with an analysis attributed to the tool and findings already confirmed, refreshed on every scan. Config `"vulnerabilities"` (default true) turns it off. **Tests first (fakes):** two vulnerable packages become two confirmed findings on the manifest; a later scan replaces them; a failed audit keeps the previous findings and adds a warning; disabled means no unit at all. | `[x]` | claude 2026-09-13 | Adds `UnitKind.Dependency`. The queue never offers one, in the ledger's own query, and `run --kind dependency` is refused, so these units are records rather than work (D06 holds: the loop still does not branch on kind). `RecordAnalysisAsync` gained an optional verdict so findings a tool settled arrive already confirmed, which is what puts them in the fix list without an agent call. A dependency unit is retired only when its manifest leaves the tree, so an audit that cannot run keeps what the last one knew. |
| T15.4 | `scan`, `status`, and `report` call them out: a line per severity after a scan, a `dependency` row in status, and a report section listing each package with its advisory and fixed version, above the code findings. **Tests first:** golden report section; status counts; scan prints the summary when anything is vulnerable and stays quiet when nothing is. | `[x]` | claude 2026-09-13 | Fix mode needs no change: they are confirmed findings on the manifest, so `fix` already plans them (D37). The report gives them their own section above the code findings and leaves them out of the severity groups, since an advisory is not a code defect; `status` counts the ones not yet fixed. Proved on ToolbagCRM 2026-09-13: 7 advisories across `toolbagcrm-ui/package.json` and `ToolbagCRM.slnx`, 2 critical, 3 high, 2 medium, in about 15 s of the scan and zero agent calls. Running it there found the audit was seeing only the C# solution: lockfiles are excluded as code, so the npm detector never saw them. The audit now reads lockfiles too, while the repository's own `exclude` globs still win, so an excluded folder is never audited. End-to-end tests turn the audit off after `init`, so the suite never reaches the network. |

---

## Later (not MVP, in rough order)

| ID | Task | Status | Notes |
|---|---|---|---|
| L1 | GitHub Action: restore ledger from cache, `scan`, `run`, `report`, coverage as a check run. | `[ ]` | Part 1.5 of `idea.md`. |
| L2 | SARIF export for GitHub code scanning. | `[ ]` | |
| L3 | Frontend-to-backend join: link a TS API-client leaf to the C# endpoint slice for true end-to-end units. | `[ ]` | Ledger must not preclude it; `Edge.kind` gains `Http`. |
| L4 | Non-git folders (own walk + ignore rules; stat cache already exists). | `[ ]` | D13. |
| L5 | Python and Go mappers. | `[ ]` | |
| L6 | Shared, versioned lenses (tenancy, a11y, OWASP). | `[ ]` | |
| L7 | Hosted scheduled scans (Part 2 of `idea.md`). | `[ ]` | Only after L1 has users. |
| L8 | Monetization conversation. | `[-]` | Deferred by Tim (D22). Remind him after Milestone B. |
| L9 | npm wrapper package `codemuster`: postinstall downloads the platform binary from the GitHub release so `npx codemuster` works anywhere Node is. Placeholder 0.0.1 in `npm/` reserves the name. | `[-]` | Superseded 2026-09-11 by D33 and T12.5: the repository is private, so platform builds ship as npm packages instead of GitHub downloads, and there is no NuGet tool to reserve. D23. Name reserved: `codemuster@0.0.1` placeholder published to npm by tim on 2026-09-10 (README only, public). Domain `codemuster.com` and GitHub org/repo also held. Next real release must be >= 0.1.0. Still to reserve: NuGet id `CodeMuster` with the first tool push in T12.1. |
| L10 | Test-strength follow-ups from the Milestone A review, not yet verified: stale marker follows fingerprint not status; CodexAdapter temp-file round trip; `--attempts` usage and give-up exit code end to end; HeadlessProcess error test proves stderr is in the message; cancellation kills the process tree, not only the shell; non-ASCII through agent stdin and stdout; FakeAgentAdapter `## Files` gate; Windows PATH walk order; Run total counts pre-existing Stale and Failed units; `AgentAdapters.Create` maps each harness; Done-rejected responses given up in Run. | `[ ]` | Each is one small test; verify the gap exists (mutate the code, watch the suite stay green) before adding it. |
| L12 | The report merges findings on the same path with overlapping lines that different units reported, listing every unit that found it. | `[ ]` | Raised 2026-09-11 by the first real slice-mode run: the `GET /quotes` and `ReminderWorker` slices both reach `QuoteRepository.ListForTenant` and each reported its missing tenant filter, so the report lists it twice. Each copy is also verified separately. |
| L13 | The TypeScript mapper names an unresolved immediately invoked function `(anonymous function)` instead of its whole source text. | `[ ]` | Raised 2026-09-11 by the first ToolbagCRM scan, whose top unresolved names held entire React components written as IIFEs. |
| L14 | A shipped lens library documented in the README: `ux` (missing loading, error, and empty states, destructive actions without confirmation, double-submit), `a11y` (alt text, label association, button semantics, focus), `copy` (typos, inconsistent terms, placeholder mismatches), and `react` (effect dependencies, stale closures, keys). | `[ ]` | Raised 2026-09-12. Config already scopes lenses by glob and language, so this is text and documentation, not code. A lens's text is part of each unit's hash, so adding one re-audits the units it applies to. |
| L15 | Locale parity as a deterministic check rather than a lens: keys present in one locale file and missing or untranslated in another. | `[ ]` | Raised 2026-09-12. Comparing `en.ts` with `es.ts` crosses two units, and a diff needs no model. |
| L11 | Keep installed skill copies current across updates: `skill install` writes a short stub that runs a new `codemuster skill show` for the full instructions, and the skill hands big repos to `run`. | `[ ]` | Raised with the update design (D33). A stale copy degrades gently for now, because every pack carries its own response shape and `done` command. |
| L16 | `fix` runs entirely off the main checkout: one integration worktree pinned to the run's base commit, where patches are applied, the test command runs, and each repair is committed, handed back as a branch to review or merge. The main checkout is never read, locked, or required to be clean, so other agents and the user can keep working during a repair run. Needs a configured prepare command (`dotnet restore`, `npm ci`) because a pristine worktree has no `obj/`, `bin/`, or `node_modules/`; opt-in before default. | `[ ]` | Raised 2026-09-20 by Tim after a `fix --agent claude -j 20` on ToolbagCRM aborted at 72 s: a concurrent Claude Code session added plugin permissions to `RoleTemplates.cs` mid-run, the out-of-scope guard in `Fix.IntegrateAsync` saw a file it had not authorized, and 19 in-flight workers were cancelled. No lock can prevent this: D46's coordinator lock serializes CodeMuster commands, not other agents writing to the same checkout, so isolation is the only real answer. Three things fall out for free: the clean-tree precondition and the whole stash path (`--stash`, the y/N prompt, recovery stashes) stop being needed; the base commit stops moving under the run, since today each repair commit advances the HEAD later workers were built against; and the out-of-scope guard becomes sound rather than a guess, because nothing but the test command writes in that worktree, which removes the misattribution in FIX2 structurally instead of rewording it. The open problem is the validation environment, and it decides the design. Tim's own `test_command` is `dotnet build toolbagcrm-api/ToolbagCRM.slnx --no-restore`, which fails instantly in a pristine worktree. Reusing the main checkout's `obj/`, `bin/` and `node_modules/` by symlink is the tempting answer and the wrong one: it reintroduces the exact collision the feature exists to prevent, one layer lower, because the concurrent session this protects against is often itself building. So a `prepare_command` run once per fix run is the honest design, and its cost is real. Merge-back is the second cost: a long run whose base has been overtaken produces a branch that may not merge cleanly, a failure mode that does not exist today. Keep it opt-in (`fix --branch <name>`) until the prepare step is proven, because defaulting it breaks every user whose test command assumes a warm build tree. |

## License update (user-directed priority)

| ID | Task | Status | Notes |
|---|---|---|---|
| LIC1 | Replace MIT with personal/internal company use terms and ship matching license metadata. | [x] | codex 2026-09-14. All 16 npm tests and dotnet test pass locally; no publication. Prioritized at Tim's explicit request over the next feature. Staging test failed because LICENSE was missing. |

## Phase 16: Complete the agent workflow (Tim's requested priority)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| T16.1 | Init agent selection, idempotent skill/hook installation, lightweight change notifications and scan acknowledgement. Tests first: preserve settings, invalid config, opt-outs, selected agents, linked worktrees and notification races. | `[x]` | codex 2026-09-14 | D42. Three CLI regressions and change-tracking regressions failed first; all 803 tests pass. Notifications live in worktree Git metadata; content fingerprints avoid read-only false alarms. |
| T16.2 | Retry declines with an explicit related-file scope, verify current findings and record resolution, and expose final validation. Tests first: outcome transitions, resumed completed files, scope isolation and validation failures. | `[x]` | codex 2026-09-14 | D43. Resolution, recovery-option, and incomplete-response regressions failed first. All 811 tests pass; current-code rechecks, SQLite outcome transitions, scoped Git commits, retry exclusions and final validation covered. |
| T16.3 | Update shared skill/help/user guide for setup, CLI-owned repairs, completion and final checks; validate installed workflow. | `[x]` | codex 2026-09-14 | Supersedes instructions to manually fix from the report. Help regressions failed first; all 813 .NET tests, 16 launcher tests and the skill validator pass. A temporary-repository CLI smoke recovered a decline with a two-file commit, rechecked current code after scan, recorded resolved/fixed, and passed a real build/runtime assertion. |

## Skill setup (user-directed priority)

| ID | Task | Status | Notes |
|---|---|---|---|
| SK1 | Skill attempts npm installation when the CLI is missing and gives manual instructions on failure. | [x] | codex 2026-09-14. Prioritized at Tim's explicit request. Regression test failed first; 800 .NET and 16 npm tests pass. Both repository skill copies refreshed. Fresh-machine agent installation not exercised. |

## Marketplace distribution (user-directed priority)

| ID | Task | Status | Notes |
|---|---|---|---|
| DIST1 | Package the shared skill for Claude/Codex marketplaces, validate distribution in CI, and document marketplace-first use alongside the CLI. | [x] | codex 2026-09-14. Tim explicitly prioritized distribution. Staging tests failed first; 815 .NET and 22 npm tests pass after syncing main. Claude/Codex manifests validated and installed in isolated profiles; release archives verified. Version 0.2.7 prepared locally; publication and live reviewer cases pending. |

## Main synchronization (user-directed priority)

| ID | Task | Status | Notes |
|---|---|---|---|
| SYNC1 | Pull main and reconcile the local skill/plugin distribution work. | [x] | codex 2026-09-14. Fast-forwarded four commits to d65558c; resolved six documentation/instruction conflicts, preserved upstream D42/D43, and renumbered local decisions D44/D45. Real init smoke caught mutually exclusive flags; corrected and regression-tested the documented command. 815 .NET and 22 npm tests pass; stashes retained, no push. |

## Launch remediation (user-directed priority)

| ID | Task | Status | Notes |
|---|---|---|---|
| LIVE1 | Close launch-readiness findings and verify the complete setup. | [~] | codex 2026-09-14. Tim explicitly requested all review items ahead of backlog. Local remediation complete: 831 .NET/28 npm tests, final Windows package smoke, 0.2.0 upgrade/pin and live Claude/Codex repair validation pass. GitHub protections enabled. Stages 15-16 remain open for npm auth (E401), hosted candidate validation and published-plugin acceptance. No commit/push/publication; existing work preserved. |

## Setup and hook utilization (user-directed priority)

| ID | Task | Status | Notes |
|---|---|---|---|
| HOOK1 | Pull latest main and investigate how setup/hooks should be utilized. | [x] | codex 2026-09-15. Taken ahead of deferred backlog at Tim's request. Main already matches origin/main at d65558c; existing work and both stashes retained. Reviewed implementation and official agent docs; 41 selected tests pass with zero skips. Disposable CLI smoke confirms no-hook freshness detection and staging-only false warnings. docs/setup-hooks-review.md proposes checkpoint reminders and independent hook setup. Investigation complete; proposed behavior is not implemented or adopted as a new decision. |
| HOOK2 | Implement proactive plugin use controlled by repository automation settings. | [x] | codex 2026-09-15. User-directed priority; D47. Strict modes, scoped interactive packs, settings-aware plugin context hooks, worker suppression, shared skill workflow, and review/repair queue separation implemented. All 862 .NET and 40 npm tests pass with zero skips; formatting, validators, parity and diff checks pass. Existing work/stashes preserved; actual live plugin acceptance and publication remain unverified. |
| REVIEW1 | Add conservative dead-code analysis and UI-only readability/business-workflow reviews. | [x] | codex 2026-09-15. User-directed priority; D48. Implemented protected static usage assessments, UI scope, current browser/screenshot evidence, mandatory flow/validation/rendering/feedback/text checks, generated findings, retained receipts, and repair/verdict safeguards. All 1,094 .NET and 40 npm tests pass with zero skips; formatting, validators, parity and diff checks pass. Real browser plus CLI ingestion captured contrast, payment-flow and pressed-feedback defects and rejected a wrong screenshot hash. No new dependencies or release actions. Live host-plugin, representative UI repair, hosted OS and publication gates remain separate. |
| INT1 | Integrate the validated plugin, repair and application-review candidate for protected main delivery. | [x] | codex 2026-09-15. Tim requested commit/push to main. This integration task consolidates the already-tested, interdependent SK1/DIST1/LIVE1/HOOK2/REVIEW1 source and generated artifacts instead of constructing partial historical commits from shared hunks. The combined 1,094 .NET cases and refreshed 40 npm cases pass, zero skips; formatting, validators, parity, diff and NuGet advisory checks pass. Main integration must retain the required three-OS CI gate. Package publication and live application acceptance remain separate. |
| REL028 | Publish version 0.2.8 for testing the proactive plugin and application reviews. | [x] | codex 2026-09-15. Release commit/tag bfe1290 is on main. Fixed a macOS regex timeout with 25 new regressions; all 1,119 .NET and 40 npm tests pass, zero skips. Tagged run 35015077715 passed three-OS validation, six builds and three package smokes. Repaired six missing trusted publishers; attempt 2 published the original packages and GitHub assets. All nine GitHub SHA256 digests and all seven npm SHA512 integrities/latest tags verify; Windows ARM64 finished npm processing at 21:05 UTC. Fresh Windows x64 registry install and full packaged-runtime smoke pass. Preserve the tag and original artifacts. Live proactive host and representative UX pilot work remains separate. |

## Audit queue resilience (user-directed priority)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| RUN1 | Exclude Markdown and continue headless reviews after an individual pack fails. Tests first: root/nested/mixed-case Markdown exclusions; retire legacy Markdown units without losing history; oversized units before/between/after valid units, serial and parallel continuation, incomplete status and successful retry; real CLI exit/coverage. | `[x]` | codex 2026-09-19 | Prioritized at Tim's request; D49. Regressions failed first; all 1,131 .NET and 40 npm tests pass, zero skips. Formatting and diff checks pass. Source changes only; not committed or released. |
| RUN2 | Exclude non-code document/data/database formats and persist oversized skips without retries. Tests first: format matrix and preserved source files; dependency audit preservation; skip persistence, queue continuation, report/status visibility and explicit retry; interactive next skips forward. | `[x]` | codex 2026-09-19 | Prioritized at Tim's request; D50. New regressions failed first. All 1,192 .NET and 40 npm cases pass after updating exclusion/schema expectations; formatting and diff checks pass. Mapper metadata, dependency audits and stored history are preserved. Tim authorized delivery with 0.2.9; RUN2 includes the superseded RUN1 implementation. |

## Version 0.2.9 release (user-directed priority)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| REL029 | Commit/push the non-code exclusions and persistent skip handling to protected main, publish npm 0.2.9, and verify registry artifacts plus a fresh install. | `[x]` | codex 2026-09-19 | PR #5 merged as 1888754; immutable v0.2.9 release run 35469006600 passed three-OS validation, six builds, three installed-package smokes and publication. All seven npm versions/latest tags and SHA512 integrities match; all nine GitHub asset sizes/SHA256 digests match. Fresh macOS arm64 registry installation passed the CLI workflow and 0.2.8 upgrade/skip/history acceptance. Receipts retained under TestResults/release-0.2.9/. |

## Update visibility (user-directed priority)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| UPDATE1 | Ship a versioned changelog, display intervening release notes after updates, and report update availability on every normal command. Tests first: multi-version notes, missing notes, staging/packing, newer/current/offline/timeout status, unchanged stdout/exit codes, opt-outs and daily background installation. | `[x]` | codex 2026-09-19 | Prioritized at Tim's explicit request; D51. Regressions failed first; 61 npm tests pass. All seven staged packages include CHANGELOG.md; stable staging requires an entry. Binary-only self-update behavior is preserved and the launcher upgrade requirement documented. Source only; no new release. |
| START1 | Preview provider/model/thinking and worker limit before agent commands; add a ten-second interactive countdown with immediate start and cancellation. Tests first: explicit/default settings, timeout, Enter/Escape/Ctrl+C, CI/redirection and real terminal cancellation before agent work. | `[x]` | codex 2026-09-19 | D52. CLI regressions failed first. Real terminal fixtures verify the full countdown, Enter skip, Esc/Ctrl+C cancellation before ledger changes, and CI bypass. Model defaults are labeled honestly. CONFIG1 shares the preview; non-agent commands do not wait. Included in 1,223 passing .NET tests. |
| CONFIG1 | Add intelligent-config to inspect repo context through an agent and apply validated configuration additions with a backup. Tests first: context and bounded samples, scoped exclusions/lenses, detected test commands, existing/unknown setting preservation, no-op, malformed/unsafe responses, cancellation/concurrent edits, atomic replacement and CLI integration. | `[x]` | codex 2026-09-19 | D53. CLI regressions failed first; application, infrastructure and fake-agent CLI tests pass. Real terminal cancellation preserves config and creates no ledger/backup. All 1,223 .NET cases pass after the full Application rerun for a corrected xUnit style assertion; formatting/diff checks pass. No live AI call or new release. |

## Version 0.3.0 release (user-directed priority)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| REL030 | Integrate update visibility, agent startup preview and intelligent configuration; publish npm 0.3.0 through protected main and verify the installed release. | `[x]` | codex 2026-09-19 | Tim requested release ahead of backlog; consolidates validated UPDATE1/START1/CONFIG1 shared hunks following INT1. PR #7 merged as d43cb8f; immutable v0.3.0 run 35473378759 passed three-OS tests, six builds, three package smokes and publication. All seven npm latest tags/SHA512 integrities and nine GitHub asset SHA256 digests verify. Fresh macOS arm64 install passed the full CLI smoke, intelligent-config backup/no-op/rejection acceptance and real registry release-note output. All 1,223 .NET and 61 npm tests pass. Older launchers need one-time npm installation for JavaScript features. Evidence: TestResults/release-0.3.0/. |

## Terminal presentation (user-directed priority)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| CLI1 | Add a compact terminal banner, prominent agent settings, colored progress and elapsed activity feedback. Tests first: terminal/plain output, NO_COLOR/CI/dumb terminals, countdown controls, progress success/failure/cancellation and cleanup. | `[x]` | codex 2026-09-19 | Prioritized at Tim's request; D54. Preview and outcome regressions failed first; all 1,241 .NET tests pass, plus 27 focused cases after the final foreground-color adjustment. Real terminal fixtures verify colors, NO_COLOR, narrow layout, failure/cancellation cleanup, Enter skip, ten-second timeout, unchanged config/ledger, and CI/dumb fallback. Formatting and diff checks pass. Existing untracked setup/config files preserved. Source only; not released. |

## Version 0.3.1 preparation (user-directed priority)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| REL031 | Prepare CLI1 as version 0.3.1 with matching plugin metadata and dated release notes. | `[x]` | codex 2026-09-19 | Tim requested a version increase and npm publication; direct main delivery, no PR. All 1,241 .NET source tests and 61 npm tests pass; plugin parity and diff checks pass. Push-triggered validation on release/** branches enables required checks before direct main pushes without PRs (manual dispatch does not satisfy GitHub protection); tagged release workflow gates publication; retain immutable archives and registry/fresh-install receipts under TestResults/release-0.3.1/. |

## Effective agent settings (user-directed priority)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| MODEL1 | Resolve Codex model and thinking before the preview and use those exact settings for every worker. Tests first: effective config, overrides, model defaults/pagination, lookup failures/cancellation, subprocess cleanup and CLI integration. | `[x]` | codex 2026-09-19 | Tim rejected provider-default placeholders; D55. Isolated from the active local fix run. Tests failed first; all 1,256 .NET tests and 61 npm tests pass, plus formatting and diff checks. Subprocess tests wait for readiness before cancellation and allow startup time under load. Live read-only Codex lookup and an empty-repository fix command display gpt-6-astra/high without agent work. Source only; not published. |

## Version 0.3.2 preparation (user-directed priority)

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| REL032 | Prepare MODEL1 and the completed audit repairs as npm/plugin 0.3.2 with matching release metadata and combined-source validation. | `[x]` | codex 2026-09-19 | Tim explicitly requests direct main delivery and npm publication, after his fix run finishes. Integrated its 30 commits without changing untracked setup/configuration files. All 1,256 .NET tests and 61 npm tests pass; formatting, plugin parity and diff checks pass. Hosted OS checks gate main; the immutable release tag gates builds, installed-package smoke checks and publication. Retain registry, archive-integrity and fresh-install receipts under TestResults/release-0.3.2/. |

## Windows cancellation release correction

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| SIG1 | Preserve graceful Windows Ctrl+C cancellation while forwarding launcher-only SIGINT on Unix. Test the signal routing and cleanup first, then prepare 0.3.3. | `[x]` | codex 2026-09-19 | The Windows signal-routing regression failed first; its correction and Unix forwarding/cleanup pass. All 64 npm tests and 1,256 .NET tests pass; plugin parity and diff checks pass. Cancelled 0.3.2 before any build or publication; preserve its tag. 0.3.3 metadata and release notes are prepared for authorized direct-main delivery and the gated npm workflow. Receipts: TestResults/release-0.3.3/. |

## Repair queue resilience

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| FIX1 | Skip an oversized fix unit instead of aborting the whole repair run, re-check skipped fix units against the current budget on every later run, and word the skip reason per unit kind. Tests first: an oversized file among eligible files is skipped serially and in parallel while the others are still fixed; the skip is persisted with its reason and spends no attempt; a still-oversized unit is skipped again; a raised `slice_token_budget` fixes it on the next run with no flag; the fix reason says to run `fix` again while the scan reason still says to rescan; the CLI reports the skipped count and exits zero. | `[x]` | claude 2026-09-20 | Tim reported `fix --agent claude -j 20` on ToolbagCRM exiting with zero files fixed because one oversized file at the head of the queue threw `PackTooLargeException` out of `Fix.RunParallelAsync`; D50 gave `run`, `verify` and `next` this behavior and `fix` was missed. New decision D56 narrows D50's sticky skip for fix units only, since their size check spends no agent call and no rescan can requeue them. Mirrors `Run.cs`: `ILedger.SkipUnitAsync` plus a `skipped:` note, surfaced as `FixResult.Skipped` and in the CLI summary. Exit code stays 0 for skips, matching `run`. `fix --force` was built on the retry-declined reopen path and then removed: a completed fix unit either has nothing unresolved, so the planner marks it done again before a worker sees it, or its unresolved findings are declined, which `--retry-declined` already reopens. Tracing that exposed a real leak the flag was masking, fixed here: a fix unit's fingerprint is the scanned content hash of its file and `fix` never rescans, so a confirmed finding recorded after the file's repair completed kept the unit done and was silently never repaired. A completed fix unit now reopens for a confirmed finding it never answered. `PackTooLargeException` now takes the unit kind, so fix units are told to run `fix` again instead of to rescan. Regressions failed first with the exact reported message and a nonzero CLI exit. Source and 0.3.4 release metadata only; not committed or released. |
| FIX2 | Two defects exposed by the same ToolbagCRM run. (a) One file's integration failure aborts the whole repair run: `Fix.RunParallelAsync` awaits `IntegrateAsync` with no try/catch and `IntegrateAsync` rethrows, so the `finally` cancels the worker token and every in-flight worker dies; 20 agent calls were paid for and none kept. Drain instead: the failure deliberately leaves the tree dirty (`preserve = true`), so the workers that already finished cannot be integrated onto it; stop dispatching new units, let the agent calls already in flight finish rather than cancelling them mid-call, retain each finished worker's copy unapplied and report its path, quarantine the one file, and exit nonzero. (b) The out-of-scope message asserts a cause it cannot know, saying `validation changed files outside the allowed scope` when CodeMuster only diffs the tree before and after and cannot tell the test command apart from a concurrent writer. Word it as an observation and name both possibilities. | `[x]` | claude 2026-09-20 | Raised 2026-09-20. Evidence that the writer was external, not a worker: all ten retained worker worktrees had changed exactly one file each, their own; `dotnet build --no-restore` cannot create a timestamped EF migration and the repository has no build-time codegen; and the migration was written after CodeMuster had already cancelled every worker. Note (a) has a real reason to stop, since `preserve = true` deliberately leaves the tree dirty and the next integration would fail its `IsCleanAsync` check anyway, so the fix is to drain and stop cleanly, not to carry on applying patches onto a dirty tree. L16 removes the cause of both; these are worth fixing first because L16 is larger and opt-in. claude 2026-09-20: drained rather than cancelled. The loop now catches `IntegrateAsync`'s throw, clears `pending`, adds the quarantined unit to `GaveUp` so the CLI still exits 1, and lets every running worker finish its call; its finished repair stays in its worker copy and the path is reported, because `IFileFixer.ReleaseAsync` now removes a worker copy only once the coordinator has used or rejected its patch. No time bound on the drain and no new `FixResult` field: Ctrl+C remains the user's bound, and `notes` plus `GaveUp` already carry the quarantined file and the retained paths. The drain regression failed first with the whole run aborting; all 1,263 .NET and 64 npm tests pass, formatting is clean. |

## Version 0.3.6 review fixes (user-directed priority)

Tim chose these fixes from `docs/product-value-review.md` on 2026-09-25, ahead of T13.10 to T13.12 and the L items. Each ID in the notes is a verified entry in that review; the reproduction for each is in the ignored `TestResults/value-review-2026-09-25/verification.json`. Every task must pass on Windows, macOS and Linux: tests first, then the full suite locally, then the three-OS test workflow on `release/0.3.6` and a `release.yml` dry run before the tag.

| ID | Task | Status | Owner / Date | Notes |
|---|---|---|---|---|
| VALUE1 | Review v0.3.5 for bugs, CLI look and feel, setup and configuration, performance, security and product features, and verify every bug report. | `[x]` | claude 2026-09-25 | `docs/product-value-review.md`. 152 distinct bug reports, each given one reproducer in an isolated worktree and two skeptics: 143 confirmed (11 high), 8 design questions, 1 refuted. No real model was called. The proposals outside the rows below are not tasks yet. |
| EXEC1 | Start `git` and `node` through `ExecutableResolver`, and skip `PATH` entries that are not fully qualified, so a `git` or `node` program in the repository or the current directory never runs. **Tests first:** a fake `git` and `node` in the working directory are not run (on Windows with `NoDefaultCurrentDirectoryInExePath` unset); relative `PATH` entries are skipped; on Linux and macOS a file without the executable bit is not chosen. | `[x]` | claude 2026-09-25 | Review `security-1`. `GitProcess` resolves `git` through `ExecutableResolver` on every call (no static state), and `TypeScriptMapper` takes a `Func<string>` for `node` rather than the suggested string path, so it resolves only when a `tsconfig.json` needs mapping and a C#-only repository still scans on a machine without Node.js. `ExecutableResolver` skips `PATH` entries that are not fully qualified (`.`, empty, `node_modules/.bin`), keeps `PATHEXT` on Windows, skips a file without an execute bit on Linux and macOS, and says where to get `git` or `node` when either is missing. Failed first: a relative entry resolved to `.\codemuster-tool-…`, only `.` on `PATH` threw nothing, and with a copy of `whoami.exe` planted as `git.exe` and `node.exe` `status` exited 1 instead of 2 and `doctor` did not start with `git: working`. Added four `ExecutableResolverTests` and `PlantedProgramTests`; `CliProcess` removes `NoDefaultCurrentDirectoryInExePath` from every CLI run, resolver tests give expected files the execute bit, and the TypeScript tests find `node` through `TestPaths.Node`. Cross-platform: Windows plants a copy of `whoami.exe` and relies on `CreateProcess` searching the current directory; Linux and macOS plant a `#!/bin/sh` script (LF, mode 700) that would leave a marker file, because .NET checks the current directory before `PATH` there; `File.GetUnixFileMode` is called only off Windows, and expected paths come from the same injected directory string, so the macOS `/private` symlink cannot matter. The Unix execute-bit test could only be run on Windows here. |
| SNAP1 | Change snapshot: hash untracked files in one `git hash-object --no-filters --stdin-paths` call, skip untracked nested repositories and worktrees, count only files scan would review, name what changed instead of blaming a hook, make the hook file write survive concurrent hooks without failing the agent's tool call, and add PowerShell to the Claude hook matcher, repairing an existing CodeMuster matcher in place. **Tests first:** a nested repository and a worktree, `report --out` followed by `status`, a bounded number of git processes with many untracked files, concurrent hooks, the matcher. | `[x]` | claude 2026-09-25 | Review `fix-commits-review-1`, `core-loop-4`, `core-loop-5`, `concurrency-cancel-8`, `cross-platform-text-11`. Unlike the row, untracked files are dropped from the snapshot rather than batched: scan reviews only the index, so a warning about an untracked file cannot be acted on, and dropping them removes the nested repository, worktree, cost and false-warning cases at once. The snapshot holds each tracked file the Cli's include predicate accepts (`Config.ExcludedReason`) with its index blob id, and a dirty file's id as `git add` would stage it, with git's clean filters rather than `--no-filters`, so staging or committing the scanned content is not a change (the lane used `git hash-object --stdin-paths`; an integration follow-up switched to HASH1's `GitBlobHasher` (D57), because `hash-object` misses git's rule that a file committed with CRLF is not converted); `IChangeTracker.HasChangesAsync` became `ChangesAsync`, returning `ScanChanges`, and status, report, fix and verify name up to three changed paths. The hook's file write retries, always deletes its temporary file and counts losing to a concurrent writer as success, and `codemuster hook` warns and exits 0 when it cannot record the change. `init` writes the plugin's matcher `^(Edit|Write|apply_patch|Bash|PowerShell|NotebookEdit)$` for Claude and Codex and repairs an existing CodeMuster handler in place. Failed first: a nested repository and a worktree made `hash-object` exit 128 and `status` exit 1, 500 untracked files reported a change after `report --out audit.md`, 1 of 16 concurrent hooks exited 1 with "Access to the path is denied", a hook exited 1 when `.git/codemuster` was a file, and the matcher lacked PowerShell and was never repaired. Added seven `GitChangeTrackerTests`, three `ChangeWarningTests` and the `AgentSetupTests` matcher and repair cases; ported the tracker test to `ChangesAsync` and updated the hook wording in `InitAgentTests`. Cross-platform: git output is read with `-z` and compared ordinally; the autocrlf test sets `core.autocrlf=true` explicitly; the hook-failure test makes `.git/codemuster` a file, which fails the same way on every OS; `File.Move` is an atomic rename on Linux and macOS and the retry covers the Windows replace race; `--git-path` results are resolved against the repository root, so the macOS `/private` temp paths do not matter. Review follow-up: a scanned deletion that was then staged or committed, and a scanned symlink retarget that was then staged, warned until the next scan because the snapshot stored "missing" or "link:<target>"; a deleted dirty file now leaves the snapshot and symlinks go through the same git hasher as files. Review follow-up: the snapshot now counts what scan actually reads (reviewed files, the .sln, .slnx, .csproj and tsconfig.json files the mappers load, and package.json while the audit is on) and passes git's linguist-generated attribute to the predicate, so a regenerated file no longer warns and a project or tsconfig edit does; lockfiles still do not count, as ChangeWarningTests pins. Review follow-up: a changed file git cannot read (held open with no sharing on Windows, mode 000 on Unix, or a required clean filter that fails) made status, report, fix and verify exit 1; the change check now degrades to one warning and the command runs, while scan still stops because its own hashing needs that file. Review follow-up: reading .git/codemuster/scanned or changed while another process replaced it failed on Windows with a sharing violation because only the writer retried; the reader now retries the same bounded way. Review follow-up: next and run, which work from the stale map, now print the same change warning on stderr (the review's core-loop-5 fix asked for it and SNAP1 had left it out); next's pack on stdout is unchanged. Review follow-up: a null entry in config exclude failed every command as an unnamed NullReferenceException, and since the snapshot runs the exclusions over every tracked file it also broke status, report, fix and verify; ConfigJson now rejects null or blank exclude and lens globs with a message naming the key. Review follow-up: the hook hashed every tracked file on each agent tool call into a notification whose content nothing read, so it now writes a constant marker once (D58, which records the refinement of D42 and waits on Tim's agreement). Review follow-up: init printed "installed <agent> skill and change hook" whether it added, repaired or left the hook alone and never said it rewrote a settings file without its comments; AgentSetup now reports each agent's outcome and init names the file it rewrote or says the skill and hook were already current (resetting a user's larger timeout to the default is unchanged and left for Tim). |
| TSAPI1 | A `typescript` package with no JavaScript compiler API (TypeScript 7) falls back to `@typescript/typescript6` when it is installed, and otherwise fails with one line naming the tsconfig, the version and the fix, instead of a `TypeError` from a deleted temp script. **Tests first:** a fake `typescript` package that exports only its version. | `[x]` | claude 2026-09-25 | Review `gap3-2-1`. `map.js` loads the compiler for every tsconfig before mapping: when `ts.createProgram` is not a function it uses `@typescript/typescript6` resolved from the same project, and otherwise fails with one line such as `web/tsconfig.json: typescript 7.0.2 has no JavaScript compiler API; run npm i -D @typescript/typescript6 --prefix web`, which doctor and scan show unchanged. Any other failure is its message plus the line in `map.js`, not a stack through the deleted temporary script. Failed first: a fake `typescript` that exports only its version gave `TypeError: Cannot read properties of undefined (reading 'readFile')` with the temporary `map.js` path, the `@typescript/typescript6` re-export failed the same way, and an unexpected failure named the temporary script. Added three `TypeScriptMapperTests`; the commit also gives EXEC1's Windows-semantics `ExecutableResolverTests` files the execute bit so they pass on Linux and macOS hosts. Cross-platform: the fake packages are written at test time and the fallback re-exports the fixture's real typescript through a JSON-quoted absolute path, so Windows backslashes stay valid; frames are matched on `__filename`, which Node realpaths the same way in its stack (including macOS `/private/var`), with a line-number pattern that fits Windows and Unix frames; `--prefix` uses `path.posix.dirname`, so it always has forward slashes. No test used the real `@typescript/typescript6` package. |
| HASH1 | Hash dirty files with git's own clean filters and object format, in one `git hash-object --stdin-paths` call, so a file keeps its hash when it is committed unchanged under `core.autocrlf` or in a SHA-256 repository. **Tests first:** with `core.autocrlf=true` a CRLF file keeps its hash from dirty to committed; a SHA-256 repository. | `[x]` | claude 2026-09-25 | Review `core-loop-6`, `gap2-1-9`. Dirty files are hashed in one batch through `IContentHasher.HashFilesAsync`, which replaces `HashFileAsync`, and `Scan`, `UxSourceSnapshot` and later `RefreshVerification` share `ContentHashes.CurrentAsync` (a clean file keeps its index id, the stat cache reuses a recorded hash); the raw-bytes `GitBlobHasher.Hash` is gone and D57 records the scheme. This commit's body describes a first `git hash-object --stdin-paths` version; the shipped `GitBlobHasher` (`update-index --add --info-only` on a temporary index copy, because `hash-object` does not read the index and misses git's rule that a file committed with CRLF is not converted) came with VERIFY1, whose commit body is the reference. Failed first: under `core.autocrlf=true` the dirty hash was `4e7cdf2b…` against the committed `0c2aa38e…`, a SHA-256 repository gave a 40-character id, the CLI regression's scan after `git commit` reported 1 stale unit, and scan hashed dirty files in 2 calls instead of 1. Added `GitBlobHasherTests` for autocrlf, SHA-256, one call for every path and no paths, a `ScanTests` batch test and `HashStabilityTests`; removed the tests of the deleted static `Hash`, and `TempRepo` can create a SHA-256 repository. Cross-platform: the tests set `core.autocrlf=true` explicitly (the Linux and macOS runners default to false) and git applies it the same way on every OS; SHA-256 needs git 2.29 or later; paths are NUL-separated, and names only Unix can create (newline, quote, trailing CR) are added only when `!OperatingSystem.IsWindows()`. |
| VERIFY1 | Refresh verification with the same members and hashes that `done` and `scan` use: an unchanged source unit keeps the reporting unit's symbol members, whole files are used only when the source changed, and dirty files are hashed as scan hashes them. **Tests first:** run, verify and scan leave no pending verify unit; committing unchanged content re-verifies nothing. | `[x]` | claude 2026-09-25 | Review `core-loop-1`, `cross-platform-text-4`. `RefreshVerification` builds each check with `PlannedUnit.Verify` over the reporting unit's own members, as `done` and `scan` do, while that unit is not retired, its fingerprint is the one the finding was reported against, and every file it covers still has the hash the last scan recorded; otherwise it falls back to whole current files, so a real edit still reopens the check. Current hashes come from the shared `ContentHashes` helper, so `IContentHasher` is now a required constructor argument. The commit also replaces HASH1's `hash-object` hasher with `update-index --add --info-only` on a temporary index copy (D57), with an optional environment on `GitProcess.RunAsync`. Failed first: an unchanged slice finding and a file verified while modified each went from Done to Pending with a new fingerprint, and the CLI regression's first `verify` after `run` completed 7 units where 0 were expected (the review's number). Added three `RefreshVerificationTests`, `VerifyTests.Verify_and_scan_redo_no_check_until_the_code_really_changes`, and `GitBlobHasherTests` for a file committed with CRLF and a linked worktree; `CompletionWorkflowTests` passes a fake hasher. Cross-platform: the Application tests use fakes; the CLI test uses the mixed-repo fixture with `core.autocrlf=false`, appends an LF line and commits with an explicit identity, since the runners have no global one; the hasher's temp index is an absolute path, so the macOS `/private` symlink and the working directory do not matter. Review follow-up: a check verify completed over whole files after an edit but before the next scan went stale on that scan (the documented fix, verify --force, scan order paid for most checks twice), and a check of a stale unit was reopened by every scan-then-verify cycle; scan now keeps a done whole-file check whose files are unchanged and adopts the reporting unit's members, and refresh restores a retired check to done when it rebuilds exactly as its last verdict saw it. |
| FIX3 | Build the worker patch with pinned flags (`--no-color --no-ext-diff --no-textconv --src-prefix=a/ --dst-prefix=b/`) so `diff.noprefix`, `color.ui=always` or `diff.external` cannot discard repairs. **Tests first:** the patch applies with those settings in the repository. | `[x]` | claude 2026-09-25 | Review `gap2-6-1`. `GitFileFixer` builds the worker patch with `diff --binary --no-color --no-ext-diff --no-textconv --unified=3 --src-prefix=a/ --dst-prefix=b/`; `--unified=3` goes beyond the row because `diff.context=0` also produced a patch `git apply` rejects. `GitWorkspace.HasFileChangesAsync` (`diff --quiet`), `ChangedPathsAsync` and the fixer's `--name-only` diff also pass `--no-ext-diff --no-textconv`, because with `diff.external` and `diff.trustExitCode=true` (git 2.46 and later) `diff --quiet` reported a changed file as unchanged and the repair was never committed. Failed first: `diff.noprefix` gave `error: c.cs: No such file or directory`, `color.ui=always` and `diff.external` gave `No valid patches in input`, `diff.context=0` gave `patch does not apply`, and `HasFileChangesAsync` returned false under `trustExitCode`. Added the four-case theory `GitFileFixerTests.UserDiffSettings_DoNotStopTheRepairFromApplyingAndCommitting` and `GitWorkspaceTests.AnExternalDiffTrustedForItsExitCode_DoesNotHideAChangedFile`. Cross-platform: the settings go in `.git/config`, which linked worktrees share on every OS; `echo` as `diff.external` exists on all three (Git for Windows ships it), so the unpinned code fails everywhere; the `trustExitCode` test only separates old from new code on git 2.46 or later and passes either way on older git. |
| FIX4 | Never record a finding Fixed when the repair changed no file: reject the attempt with a retry diagnostic, and make the fake agent make a real edit. **Tests first.** | `[x]` | claude 2026-09-25 | Review `fix-git-1`. `Fix.IntegrateAsync` rejects a response that addresses findings while the patch for its allowed paths is empty: the attempt is recorded as failed, counts toward `--attempts`, and the retry pack says "addressed findings but the file is unchanged; edit it or decline each with a reason". Decline-only answers stay valid, and the check runs before the patch is applied, so it costs no test run. In write mode the fake agent now appends `// codemuster fake fix` to C#, TypeScript and JavaScript files and a newline to anything else, so the end-to-end fix tests make real commits. Failed first: the Application test accepted the empty repair as Fixed on the first call (1 agent call where 2 were expected), the fake edited nothing, and `Fix_RecordsAnOutcomeForEveryConfirmedFinding` expected 15 commits and got 0, the reported defect. Added `FixRunTests.AddressedFindingsWithoutAChangedFile_AreRejected_AndTheRetryIsToldWhy` and two `FakeAgentAdapterTests`; seven `FixCommandTests` now expect one commit per fixed file or the fake agent's edit instead of a `git apply` test command, and `AuditedAsync` sets a repository identity with `commit.gpgsign=false` because the CI runners have no global one. Cross-platform: the fake agent combines forward-slash keys with `Path.Combine`, which Windows accepts, and appends LF text to LF fixtures (`core.autocrlf=false`); the worktree comparison uses only `worktree` lines, not HEAD ids. Review follow-up: the guard checked only the worker patch, so a repair that test_command rewrote back to the committed content (a codegen or restore step) was still recorded Fixed with no commit; after validation a response that addresses findings but leaves no change is now rejected and the file restored, which also clears the stat-dirty state Windows autocrlf left. Review follow-up: done fix:<file> still recorded addressed findings Fixed with no check of the file, so the done verb now refuses fix units and points at fix --path (fix itself records through the Done use case, not the verb). |
| FIX5 | Run `test_command` once on the unmodified tree before any agent call; if the program is missing or the run fails, stop with its last lines and no agent call. `--allow-failing-tests` skips the check. **Tests first.** | `[x]` | claude 2026-09-25 | Review `setup-config-9`. When a fix unit is pending and `test_command` is set, `fix` runs it once on the unmodified tree, after the stash and clean-tree checks and before any worker starts. A program that cannot start or a failing run stops with exit 1, no agent call, the stash restored, the command's last 20 non-empty lines and a hint; a passing baseline costs one extra test run, and `fix --allow-failing-tests` skips the check. Beyond the row, a passing baseline that changes tracked files also stops before any agent call, naming the files, since the first integration would otherwise fail after paid calls. Failed first: the Application tests saw no exception for a failing baseline, a missing program or a dirty baseline and counted one test run too few, and the CLI baseline tests found `(attempt 1/3)`, meaning agents were called. Added six `FixRunTests` and two `FixCommandTests`; existing fix tests queue a passing baseline or expect one more run, and the tests of the post-repair gate pass `--allow-failing-tests`. Cross-platform: the CLI tests fail the baseline with `git show codemuster-baseline-marker`, which exits 128 and echoes the marker on every OS and locale; the missing-program test asserts only the program name and the hint, so it holds for Windows `PATHEXT` lookup and Unix lookup alike; trailing CR is trimmed from Windows output. |
| AUDIT1 | A failed audit (offline, registry or restore error) keeps the earlier findings and warns: each parser rejects output that is not a real report. **Tests first.** | `[x]` | claude 2026-09-25 | Review `agents-processes-2`. Each audit parser now throws, carrying the tool's own message, for its error envelope or for output with no report in it (npm needs `auditReportVersion` and `vulnerabilities`, pnpm `advisories`, dotnet `projects` and no error-level problem, yarn an `auditSummary`, advisory line or advisory map and no `"type":"error"` line). `DependencyAuditor` turns that into a scan warning that says earlier findings are kept and plans no dependency unit, so the old unit and findings stay. The yarn parser also reads Yarn 3's one-line advisory map, which it used to ignore. Failed first: every parser read its error envelope as a clean report (`Assert.Throws` saw no exception), Yarn 3's map gave no packages, and `DependencyAuditorTests` recorded the npm offline envelope as a clean manifest. Added five `AuditParserTests`, one `DependencyAuditorTests` case and seven real captured envelopes under `tests/CodeMuster.Infrastructure.Tests/audits/`; `DependencyScanTests.AFailedAudit_KeepsWhatWeKnew_AndSaysWhatBroke` now also checks the finding id, unit state and status counts. Cross-platform: pure JSON parsing with an injected process runner; the fixtures are LF under `.gitattributes`, the yarn parser trims CR, and no test needs pnpm or yarn installed. Review follow-up: every unchanged scan printed each audited manifest as stale because the dependency analysis was recorded with an empty lens hash while scan plans the manifest's real one; it is now recorded with that lens hash, so the summary's stale count is true (predates 0.3.6). |
| AUDIT2 | One Node audit per folder when several lockfiles exist, chosen from `packageManager`, then the pnpm, yarn and npm lockfiles, with a warning naming the choice; `Scan` merges manifests by path as a guard. **Tests first.** | `[x]` | claude 2026-09-25 | Review `agents-processes-1`. `DependencyAuditor` makes one Node job per `package.json` folder that has a lockfile. With lockfiles for more than one tool it uses the tool `packageManager` names when that tool's lockfile is present, otherwise pnpm, then yarn, then npm, and warns naming the lockfiles and the choice; `package-lock.json` with `npm-shrinkwrap.json` is one npm audit with no warning. `Scan` merges audit results by manifest path before planning units, so a duplicate from any auditor cannot plan two units with one id. Failed first: two lockfiles gave two jobs (`Assert.Single` saw 2 or 3), and `TwoResultsForOneManifest_AreMerged_RatherThanCrashingTheScan` threw the reported `An item with the same key has already been added. Key: dependency:web/package.json`. Added four `DependencyAuditorTests` (one a three-case `packageManager` theory) and the Scan merge test. Cross-platform: lockfile names are compared ordinally against git's forward-slash paths, so case-sensitive and case-insensitive file systems agree; the `packageManager` test uses a real temp folder, asserts only the folder name, and no test needs pnpm or yarn installed. |
| PERF1 | Compile each glob once per `Config` instead of on every match. **Tests first:** matching results are unchanged for `**`, `./`, case and directory globs; a loose timing guard. | `[x]` | claude 2026-09-25 | Review `fix-commits-review-2`. `Glob` is now a compiled `NonBacktracking` matcher, and the new Domain `GlobList` compiles its patterns once, on first match, per `Config`, `Lens` and `UserExperienceSettings` instance (rebuilt in the `init` accessor, so `with` copies match the new globs); `IntelligentConfig` and `UiScope` also compile each pattern once. Matching, record equality, the `config.json` field order and lens hashes are unchanged. Failed first: `Globs_AreCompiledOncePerConfig_NotOnEveryMatch` allocated about 11 GB in 7.7 s for 3,000 paths and 10 exclude globs against a 32 MB bound (about 2 MB after). Added the compiled `Glob` and `GlobList` tests and four `ConfigTests`; the glob theory grew from 41 to 56 cases (directory globs, case, `./`, `?`) and passed on the old code. Cross-platform: pure string and regex work with no file system access, ordinal and case-sensitive on every OS; the guard counts allocations on the current thread rather than time, so a slow runner cannot trip it. |
| PERF2 | The npm launcher skips the update check for `hook` and when `CODEMUSTER_WORKER` is set. **Tests first.** | `[x]` | claude 2026-09-25 | Review `concurrency-cancel-2`. `main()` in `npm/lib/launcher.js` skips `checkForUpdate`, and the background update it can start, when the first argument is `hook` or `CODEMUSTER_WORKER` is set to any value (as the plugin hooks treat it); every other command still checks as D51 requires. The `update` help in the launcher and `HelpText.cs`, `docs/usage.md` and both READMEs name the two exemptions. Failed first: the three new `launcher.test.js` tests (hook, and a worker with `CODEMUSTER_WORKER` set to `1` and to empty) saw a registry request where none was expected. Cross-platform: the tests mock `fetch`, `spawn` and the home folder and read the injected environment rather than `process.env`, so they mean the same on every OS; workers are started with `CODEMUSTER_WORKER=1` everywhere. |
| USAGE1 | Usage errors name the problem: the reason plus the verb's usage line, `--name=value`, a suggestion for a mistyped verb, option allow-lists for `next`, `status`, `done` and `skill`, message-only errors prefixed `error:` with no `(Parameter ...)`, and `fake` left out of user-facing agent lists. `--help` output is unchanged. **Tests first.** | `[x]` | claude 2026-09-25 | Review `cli-look-feel-3`, `cli-look-feel-4`, `cli-config-dist-4`. `CommandLine.Parse` reads each verb against one table of the options, flags, required options and positional it accepts, taken from what `Program` reads (`next`: batch, out, path, kind; `done`: fingerprint, findings and one unit; `skill`: install, for, global; `status`: none; every earlier list kept). The first mistake throws `UsageException`: one line such as `codemuster run: -j must be a positive whole number (got "0")`, then the verb's usage line (the first line of its help plus `; see codemuster <verb> --help`); exit 2 as before, and a bare `codemuster` still prints the overview. An unknown option lists the valid ones and suggests one within a third of its letters (`next --pth` gives `--path`); an unknown command suggests a verb, including the launcher's `update`, and `update` reaching the binary says only the launcher can update. `--name=value` splits at the first `=`, the later repeated option still wins, and an empty value is now rejected. `AgentAdapters`, `GeminiAdapter` and `SkillInstaller` throw message-only exceptions; Program's handlers prefix `error: ` (D24's message keeps its text; `cancelled` stays bare because the user chose it); `AgentAdapters.Names` lists claude, codex, gemini, opencode and `fake` is still accepted. Not done: the review's mapping of file and git errors to argument-naming messages, and case-insensitive `--kind`. `--help` and `help <verb>` were captured before and after across 53 invocations and are byte-identical; the skill, plugin hooks and launcher depend only on help text, `nothing pending` and the launcher's own `update` usage. Failed first: 92 of 137 selected CLI tests (every mistake gave the 42-line overview; `--for=none` was an unknown option), and the adapter, Gemini and skill-installer messages carried `(Parameter ...)` and `fake`. Added parser theories for each mistake, unknown option and command, usage line and allow-list acceptance, plus CLI tests for the two-line output, exit codes, `--name=value` and the `error:` prefix; the bad-usage theories now check both lines instead of `usage: codemuster`, and `RunWithUnknownAgent` and three exception tests pin the new messages. docs/usage.md shows the messages and lists `next --kind`. Cross-platform: string work only, ordinal comparison and invariant lowercasing; tests split stderr after `ReplaceLineEndings`, so Windows CRLF and Unix LF read alike; help text is LF under `.gitattributes` and the usage line trims a stray CR; the file-error test asserts `error: ` and the file name, not the absolute path, so macOS `/private/var` cannot matter. |
| REL036 | Prepare 0.3.6: dated CHANGELOG entry, plugin manifests, the three-OS test workflow on `release/0.3.6`, and a `release.yml` dry run. | `[ ]` | | Tim fast-forwards `main`, tags `v0.3.6` and publishes. |
